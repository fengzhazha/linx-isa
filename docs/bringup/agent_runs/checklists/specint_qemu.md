# SPECint / QEMU Checklist

## Live Blockers (2026-07-01)

- [x] ID: SPEC-M05-502-RUN-INDEX-PCWATCH-20260702 Focused `502.gcc_r` train row selection and PC-watch triage are in the loop.
  Runner/tool evidence: `tools/spec2017/run_int_rate_qemu.py` supports repeatable `--run-index <n>` for 1-based SPEC command rows and records `source_run_index` while filtering compares to the selected row's declared outputs. The classifier now lets `LINX_SPEC_FAIL`/child-exit markers and timeouts override passive `linx_pc_watch:` records; reserve `pc-watch-exit` for watch traces with no stronger runtime marker.
  Focused command shape: `SPEC_QEMU_HEARTBEAT_INTERVAL=1000000000 python3 tools/spec2017/run_int_rate_qemu.py --spec-dir workloads/spec2017/cpu2017v118_x64_gcc12_avx2 --qemu emulator/qemu/build-linx/qemu-system-linx64 --stage b --input-set train --transport initramfs --bench 502.gcc_r --run-index 1 --sysroot out/libc/musl/install/phase-b --timeout 300 --append-extra norandmaps --stack-limit 2G --dump-prefix-bytes 512 --out-dir workloads/generated/specint-502-train-row1-current-20260702-r1`.
  Evidence: `workloads/generated/specint-502-train-row1-current-20260702-r1/stage_b_summary.json` reproduces the selected train command row (`200.c -O3 -finline-limit=50000`) as a late userspace correctness failure with heartbeat site progress through count `14000000009`; runtime `0x1555d65c26` maps to `pointer_set_insert+0x9e` at the rehash store, and `orig_tpc=0x1556089856` maps to musl `mmap+0xa6`. A less intrusive follow-up with guest heartbeats disabled and bounded PC-watch, `workloads/generated/specint-502-train-row1-xcalloc-pcwatch-20260702-r1/502_gcc_r/run_001/qemu.log`, reproduces the original child exit code `4` with `benchmark internal error ... tree-into-ssa.c:942`; the same log shows the watched `xcalloc` return path producing nonzero allocation pointers in the final window, so the current canonical owner is GCC data-structure/order corruption around `prune_unused_phi_nodes`/`spec_qsort`/worklist processing rather than QEMU deadlock or an immediate null `xcalloc` return.
  Loop update: use `--run-index 1` for the next `502.gcc_r` train probes. Keep QEMU heartbeat coarse and PC-watch passive unless deliberately using `LINX_DEBUG_PC_WATCH_EXIT=1`. Next probes should instrument or watch `prune_unused_phi_nodes`, `spec_qsort`, `cmp_dfsnum`, and the `worklist`/`defs` arrays near the `tree-into-ssa.c:942` assertion before changing QEMU, libc, or Linux VM policy.

- [x] ID: SPEC-M05-TEST-TRAIN-ALL-20260701 Bounded `test-train` fast gate covers every current Linx SPECint C/C++ rate row.
  Build evidence: `workloads/generated/specint-build-all-20260701-r1/build_manifest.json` reports `overall_ok=true` for `500.perlbench_r`, `502.gcc_r`, `505.mcf_r`, `520.omnetpp_r`, `523.xalancbmk_r`, `525.x264_r`, `531.deepsjeng_r`, `541.leela_r`, `557.xz_r`, and `999.specrand_ir`.
  Run command shape: `LINX_QEMU_HEARTBEAT_SAME_SITE_WARN=4 SPECINT_TEST_ALL_TIMEOUT=120 SPECINT_TRAIN_ALL_TIMEOUT=180 SPEC_GUEST_HEARTBEAT_SEC=0 SPEC_QEMU_HEARTBEAT_INTERVAL=1000000000 SPEC_NO_PROGRESS_TIMEOUT=120 LINX_SPEC_SYMBOLIZE_HEARTBEAT=1 python3 tools/bringup/run_specint_fast_gate.py --profile test-train --spec-dir workloads/spec2017/cpu2017v118_x64_gcc12_avx2 --qemu emulator/qemu/build-linx/qemu-system-linx64 --sysroot out/libc/musl/install/phase-b --out-dir workloads/generated/specint-test-train-all-mmiohole-qemu-20260701-r1 --append-extra "norandmaps" --heartbeat-sec 30 --qemu-heartbeat-interval 1000000000 --guest-heartbeat-sec 0 --no-progress-timeout 120 --stack-limit 2G --transports initramfs --symbolize-heartbeat --continue-on-fail`.
  Result: both `test-all` and `train-all` attempted all ten rows on rebuilt QEMU `v10.2.0-989-g5cfb672a711`. The run is still strict-red, but the failure ledger changed after the QEMU virt MMIO-hole fix. `500.perlbench_r` is now heartbeat-backed live-timeout on both inputs. `502.gcc_r` and `520.omnetpp_r` are live-timeout on test and user-trap on train. `505.mcf_r` still exits through the wrapper with code 255 on both inputs. `523.xalancbmk_r`, `541.leela_r`, and `557.xz_r` are live-timeout on test; train keeps `523.xalancbmk_r` and `541.leela_r` live-timeout while `557.xz_r` exits through the wrapper with child code 1. `525.x264_r` still panics before SPEC start with `VFS: Unable to mount root fs on "" or unknown-block(0,0)`. `531.deepsjeng_r` and `999.specrand_ir` reach guest `LINX_SPEC_PASS` on both inputs but fail host strict hash/output-size validation. Every live-timeout row has `heartbeat_running=true`, site changes, and BPC progress, so these rows remain throughput/live-slow rather than deadlock.
  Focused 531 evidence: `workloads/generated/specint-531-test-filesys-trace-20260701-r1/531_deepsjeng_r/run_001/qemu.log` proves the wrapper chdirs to `/spec-run`, stats and opens `/spec-run/deepsjeng_r_base.mytest-m64`, then `execve()` succeeds. The child runs for ~6B guest instructions and exits 0, but no child-side `openat`/`newfstatat`/`faccessat` for `test.txt` appears before `test.out` contains `Deep Sjeng...` followed by `Allocated Workload not found`. `workloads/generated/musl-control-stdio-cpp-20260701-r1/summary.json` passes static C `file_stdio` and `printf_string_arg` controls but fails static `cpp17_smoke` with a user trap, so the active owner is C++ runtime/codegen correctness rather than initramfs input packaging or host output collection.
  Loop update: current runner summaries classify guest-pass/output-verification failures as `hash-mismatch` instead of leaving their QEMU failure class as `none`. Use `test-train` for complete bounded ledgers; use focused single-row reruns with `LINX_QEMU_FAULT_TRACE*`, `LINX_QEMU_TLB_FILL_TRACE*`, output prefix dumps, indexed argv overrides, cwd/pre-exec probes, or syscall tracing to resolve the specific owner class before broad reruns. For speed profiling, remove verbose kernel log bootargs and use coarse or disabled heartbeat after `LINX_SPEC_START` so QEMU samples are not dominated by printk/vsprintf and heartbeat debug work.

- [x] ID: SPEC-M05-9P-STORAGEINIT-AUTO-20260630 SPEC 9p runners enable Linx storage init by default.
  Evidence: `workloads/generated/specint-train-all-tilecommit-guard-9p-20260630-r1/` showed every 9p train row failing before execution with `LINX_SPEC_WARN 9p-mount-failed raw_rc=-19 neg_errno=19` followed by `LINX_SPEC_FAIL chdir-rundir` when the command omitted `linx_storage_init=1`. The fixed runner proof is `workloads/generated/specint-999-9p-storageinit-auto-20260630-r1/qemu_matrix_summary.json`: the generated QEMU command line contains `linx_storage_init=1`, reaches `LINX_SPEC_START 999.specrand_ir`, emits no 9p mount warning, and classifies as heartbeat-backed `live-timeout` with BPC site progress in `slub.c`. The interrupted all-train follow-up under `workloads/generated/specint-train-all-storageinit-auto-9p-20260630-r1/` advanced through multiple SPEC rows with the same automatic bootarg and live BPC progress before this submit/merge handoff stopped it.
  Result: `tools/spec2017/run_int_rate_qemu.py` now appends `linx_storage_init=1` automatically for `--transport 9p` unless the caller explicitly supplies `linx_storage_init=...`; `LINX_SPEC_9P_FORCE_VIRTIO_MMIO=1` still adds the default `virtio_mmio.device=` only when the caller has not supplied one.
  Loop update: do not require ad-hoc `--append-extra "linx_storage_init=1"` in future SPEC 9p gate commands. Treat future `9p-mount-failed raw_rc=-19` as a regression in runner command construction or Linux storage init, not as a benchmark failure. The current post-fix 9p owner remains throughput: heartbeat/BPC progress and host sampling point at kernel allocator/9p overhead plus QEMU template/BSTART validation cost, not deadlock.

- [x] ID: SPEC-M05-TILECOMMIT-GUARD-20260630 QEMU skips scalar no-op tile commits in SPEC hot blocks.
  Profile command shape: run `531.deepsjeng_r` train input with heartbeat disabled, wait until `LINX_SPEC_START`, then sample the new `qemu-system-linx64` process with `/usr/bin/sample`.
  Evidence: pre-change sample `workloads/generated/specint-profile-531-current-20260630-r1/profile/qemu-531-current.sample.txt`; post-change sample `workloads/generated/specint-profile-531-tilecommit-guard-20260630-r1/profile/qemu-531-tilecommit-guard.sample.txt`; SPEC sentinel `workloads/generated/specint-train-smoke-tilecommit-guard-20260630-r1/specint_fast_gate_summary.json`.
  Result: `target/linx/translate.c` now branches around `linx_tile_commit` when `tile_iot_valid == 0`, preserving dynamic tile descriptor state while avoiding a hot no-op helper on scalar coupled blocks. In the focused `531.deepsjeng_r` sample, `helper_linx_tile_commit` falls from 185 string occurrences in the sampled call graph to zero. QEMU strict AVS all/system gates and `999.specrand_ir` train-smoke still pass. The dedicated `avs/qemu/run_tests.py --suite tile --timeout 120` timeout is reproduced on the rebuilt baseline without this guard, so it remains a pre-existing tile-suite duration/coverage lane rather than evidence against the scalar guard.
  Loop update: keep heartbeat disabled or coarse for host samples. The remaining sampled SPEC steady-state QEMU costs are template entry/exit, BSTART/probe validation, tile block reset/set helpers, and occasional post-start TLBI; do not treat boot-biased TLBI or scalar no-op tile commit as the primary live-slow blocker after this patch.

- [x] ID: SPEC-M05-TLBIV-PAGEFLUSH-20260630 QEMU page-scopes Linx virtual-address TLB invalidation.
  Smoke command: `LINX_TLB_TRACE=1 LINX_TLB_TRACE_LIMIT=24 python3 avs/qemu/run_musl_smoke.py --mode phase-b --link static --runner system --sample mprotect_adjacent --qemu emulator/qemu/build-linx/qemu-system-linx64 --timeout 90 --append "lpj=1000000 console=ttyS0 kfence.sample_interval=0 ignore_loglevel loglevel=8" --out-dir avs/qemu/out/musl-mprotect-adjacent-tlbiv-pageflush-20260630-r1`
  SPEC commands: `python3 tools/bringup/run_specint_fast_gate.py --profile pr --suite train-smoke ... --out-dir workloads/generated/specint-train-smoke-tlbiv-pageflush-20260630-r1` and `LINX_QEMU_HEARTBEAT_SAME_SITE_WARN=4 python3 tools/spec2017/run_stage_qemu_matrix.py ... --bench 502.gcc_r --out-dir workloads/generated/specint-502-tlbiv-pageflush-20260630-r1`.
  Evidence: `emulator/qemu` commit for `target/linx/{helper.c,helper.h,translate.c}`, `avs/qemu/out/musl-mprotect-adjacent-tlbiv-pageflush-20260630-r1/summary.json`, `workloads/generated/specint-train-smoke-tlbiv-pageflush-20260630-r1/specint_fast_gate_summary.json`, and `workloads/generated/specint-502-tlbiv-pageflush-20260630-r1/qemu_matrix_summary.json`.
  Result: QEMU now lowers `tlb.iv` and Linux's packed `tlb.iav` operand to `tlb_flush_page()` across all QEMU MMU indexes and clears only BSTART-cache entries on the affected page. `tlb.ia` remains a conservative full flush because this QEMU model does not keep ASID-tagged TLB entries independent of TTBR/MMU-index state. The smoke log records `LINX_TLB_TRACE op=iv ... operand=...` and still passes `MUSL_MPROTECT_ADJACENT_PASS`; `999.specrand_ir` train-smoke passes; focused `502.gcc_r` remains `live-timeout` with BPC site progress and no `LINX_USER_TRAP`.
  Loop update: keep TLBI as a boot-performance lane unless a post-`LINX_SPEC_START` host sample again identifies TLB invalidation as a steady-state SPEC bottleneck. Remaining QEMU speed work for the live-slow rows is still tile/template/BSTART/probe cost, TB chaining, wrapper polling, allocator/free-list hot BPCs, and 9p or block transport overhead.

- [x] ID: SPEC-M05-MPROTECT-NOMERGE-20260630 Linux VMA no-merge fix moves `502.gcc_r` from correctness trap to live-slow.
  Smoke command: `python3 avs/qemu/run_musl_smoke.py --mode phase-b --link static --runner system --sample mprotect_adjacent --qemu emulator/qemu/build-linx/qemu-system-linx64 --timeout 90 --append "lpj=1000000 console=ttyS0 kfence.sample_interval=0 ignore_loglevel loglevel=8 linx_vm_trace=1 linx_mprotect_trace=1 linx_mprotect_trace_limit=8" --out-dir avs/qemu/out/musl-mprotect-adjacent-nomerge-20260630-r1`
  Focused 502 command shape: `LINX_QEMU_HEARTBEAT_SAME_SITE_WARN=4 LINX_QEMU_FAULT_TRACE=1 LINX_QEMU_FAULT_TRACE_ADDR_LO=0x3f7fa8d000 LINX_QEMU_FAULT_TRACE_ADDR_HI=0x3f7fa8dfff LINX_QEMU_TLB_FILL_TRACE=1 LINX_QEMU_TLB_FILL_TRACE_VA_LO=0x3f7fa8d000 LINX_QEMU_TLB_FILL_TRACE_VA_HI=0x3f7fa8dfff python3 tools/spec2017/run_stage_qemu_matrix.py ... --bench 502.gcc_r --append-extra "norandmaps ignore_loglevel loglevel=8 linx_vm_trace=1 linx_vm_trace_addr=0x3f7fa8d000 linx_mprotect_trace=1 linx_mprotect_trace_addr=0x3f7fa8d010 linx_mprotect_trace_limit=32" --out-dir workloads/generated/specint-502-mprotect-nomerge-20260630-r1`
  Train-all command: `LINX_QEMU_HEARTBEAT_SAME_SITE_WARN=4 SPECINT_TRAIN_ALL_TIMEOUT=180 python3 tools/bringup/run_specint_fast_gate.py --profile train --suite train-all --spec-dir workloads/spec2017/cpu2017v118_x64_gcc12_avx2 --qemu emulator/qemu/build-linx/qemu-system-linx64 --sysroot out/libc/musl/install/phase-b --out-dir workloads/generated/specint-train-all-nomerge-qemu-20260630-r1 --append-extra "norandmaps ignore_loglevel loglevel=8" --guest-heartbeat-sec 0 --heartbeat-sec 30 --qemu-heartbeat-interval 1000000000 --no-progress-timeout 120 --stack-limit 2G --symbolize-heartbeat --continue-on-fail`
  Evidence: `avs/qemu/out/musl-mprotect-adjacent-before/summary.json`, `avs/qemu/out/musl-mprotect-adjacent-nomerge-20260630-r1/summary.json`, `workloads/generated/specint-502-mprotect-trace-20260630-r1/initramfs/502_gcc_r/run_001/qemu.log`, `workloads/generated/specint-502-mprotect-nomerge-20260630-r1/qemu_matrix_summary.json`, and `workloads/generated/specint-train-all-nomerge-qemu-20260630-r1/specint_fast_gate_summary.json`.
  Result: before the fix, the focused 502 trace shows `mprotect(0x3f7fa8d000, 0x1000, PROT_READ|PROT_WRITE)` returning success but leaving `prev=none cur=none next=none target=none`, followed immediately by `LINX_VM_FAULT stage=no-vma` at `0x3f7fa8d010`. `mm/vma.c` now skips `vma_merge_existing_range()` under `CONFIG_ARCH_LINX`, matching the existing Linx no-merge policy for mmap/brk/mremap until the maple-tree merge path is fixed. After the fix, the isolated `mprotect_adjacent` smoke passes and the post-mprotect trace keeps both changed and adjacent VMAs present. Focused `502.gcc_r` no longer traps; it runs to `live-timeout` with heartbeat site progress. The refreshed all-ten train gate passes `999.specrand_ir` strict hash `0x973dcfc2`; classifies `500`, `502`, `505`, `520`, `523`, `531`, `541`, and `557` as heartbeat-backed `live-timeout`; and keeps `525.x264_r` as the initramfs `VFS: Unable to mount root fs` panic.
  Loop update: keep the mprotect trace switch (`linx_mprotect_trace=1`, optional `linx_mprotect_trace_addr=<va>`, optional `linx_mprotect_trace_limit=<n>`) for focused Linux VM/VMA regressions, not broad suite runs. The SPEC QEMU runner now carries `LINX_MPROTECT` summaries into per-run JSON and matrix/fast-gate failure details. `502.gcc_r` leaves the correctness-trap lane and joins live-slow profiling. Next owners are QEMU/kernel throughput for the live-timeout rows and transport/rootfs packaging for `525.x264_r`.

- [x] ID: SPEC-M05-HBSTALL-VMTRACE-20260630 Heartbeat stall and VM-trace instrumentation split deadlock, Linux VM correctness, and throughput lanes.
  Train-all command: `LINX_QEMU_HEARTBEAT_SAME_SITE_WARN=4 SPECINT_TRAIN_ALL_TIMEOUT=180 python3 tools/bringup/run_specint_fast_gate.py --profile train --suite train-all --spec-dir workloads/spec2017/cpu2017v118_x64_gcc12_avx2 --qemu emulator/qemu/build-linx/qemu-system-linx64 --sysroot out/libc/musl/install/phase-b --out-dir workloads/generated/specint-train-all-hbstall-qemu-20260630-r1 --append-extra "norandmaps ignore_loglevel loglevel=8 linx_vm_trace=1 linx_vm_trace_addr=0x3f7fa8d000" --guest-heartbeat-sec 0 --heartbeat-sec 30 --qemu-heartbeat-interval 1000000000 --no-progress-timeout 120 --stack-limit 2G --symbolize-heartbeat --continue-on-fail`
  Focused 502 command shape: `LINX_QEMU_HEARTBEAT_SAME_SITE_WARN=4 LINX_QEMU_FAULT_TRACE=1 LINX_QEMU_FAULT_TRACE_ADDR_LO=0x3f7fa8d000 LINX_QEMU_FAULT_TRACE_ADDR_HI=0x3f7fa8dfff LINX_QEMU_FAULT_TRACE_LIMIT=32 LINX_QEMU_FAULT_TRACE_REGS=1 LINX_QEMU_TLB_FILL_TRACE=1 LINX_QEMU_TLB_FILL_TRACE_VA_LO=0x3f7fa8d000 LINX_QEMU_TLB_FILL_TRACE_VA_HI=0x3f7fa8dfff LINX_QEMU_TLB_FILL_TRACE_LIMIT=64 python3 tools/spec2017/run_stage_qemu_matrix.py ... --bench 502.gcc_r --append-extra "norandmaps ignore_loglevel loglevel=8 linx_vm_trace=1 linx_vm_trace_addr=0x3f7fa8d000" --out-dir workloads/generated/specint-502-vmtrace-uart-20260630-r1`
  Evidence: `workloads/generated/specint-train-all-hbstall-qemu-20260630-r1/specint_fast_gate_summary.json`, `workloads/generated/specint-train-all-hbstall-qemu-20260630-r1/train-all/qemu_matrix_summary.json`, `workloads/generated/specint-train-all-hbstall-qemu-20260630-r1/train-all/initramfs/stage_b_summary.json`, `workloads/generated/specint-502-vmtrace-uart-20260630-r1/qemu_matrix_summary.json`, and `workloads/generated/specint-502-vmtrace-uart-20260630-r1/initramfs/502_gcc_r/run_001/qemu.log`.
  Result: all ten supported SPECint C/C++ train rows were attempted. `999.specrand_ir` passes strict train hash `0x973dcfc2`. `502.gcc_r` is the only user-trap row and now has focused VM evidence: earlier faults on `0x3f7fa8d008` show `LINX_VM_FAULT stage=good-vma` and `stage=handled fault=0`, while the terminal store to `0x3f7fa8d010` shows `LINX_TLB_FILL_TRACE legacy_why=type0 legacy_desc=0x0`, `LINX_FAULT_TRACE mem_va=0x3f7fa8d010`, and `LINX_VM_FAULT stage=no-vma`. `525.x264_r` still panics in the initramfs lane with `VFS: Unable to mount root fs`. `500.perlbench_r`, `505.mcf_r`, `520.omnetpp_r`, `523.xalancbmk_r`, `531.deepsjeng_r`, `541.leela_r`, and `557.xz_r` are heartbeat-backed live-timeout rows with BPC/site progress. `531.deepsjeng_r` also emits `LINX_HEARTBEAT_STALL status=same-site-running repeats=4 threshold=4`, proving the same-site warning path works; later site progress keeps it out of hard-deadlock classification.
  Loop update: keep `LINX_QEMU_HEARTBEAT_SAME_SITE_WARN=4` plus `LINX_QEMU_HEARTBEAT_INTERVAL=1000000000` in bounded all-train diagnostics. Use address-filtered `LINX_QEMU_FAULT_TRACE_ADDR*` and `LINX_QEMU_TLB_FILL_TRACE_VA*` only in focused single-row runs. The next 502 owner is Linx Linux `mprotect()`/VMA lifetime around `0x3f7fa8d000..0x3f7fa8efff`, not QEMU TLB policy. The next 525 owner is transport/rootfs packaging or 9p/block-backed execution. The live-timeout rows need post-`LINX_SPEC_START` host profiling with heartbeat disabled or coarse so debug logging does not dominate the sample.

- [x] ID: SPEC-M05-TRAIN-ALL-TLBFILL-20260630 Current initramfs train-all ledger covers every SPECint row with QEMU BPC heartbeat and focused 502 page-walk evidence.
  Run command: `SPECINT_TRAIN_ALL_TIMEOUT=180 SPEC_GUEST_HEARTBEAT_SEC=0 SPEC_QEMU_HEARTBEAT_INTERVAL=1000000000 SPEC_NO_PROGRESS_TIMEOUT=180 LINX_SPEC_SYMBOLIZE_HEARTBEAT=1 python3 tools/bringup/run_specint_fast_gate.py --profile train --spec-dir workloads/spec2017/cpu2017v118_x64_gcc12_avx2 --qemu emulator/qemu/build-linx/qemu-system-linx64 --sysroot out/libc/musl/install/phase-b --out-dir workloads/generated/specint-train-all-tlbfill-debug-qemu-20260630-r1 --append-extra norandmaps --heartbeat-sec 30 --qemu-heartbeat-interval 1000000000 --guest-heartbeat-sec 0 --no-progress-timeout 180 --stack-limit 2G --transports initramfs --continue-on-fail`
  Focused 502 command shape: `LINX_TLB_FILL_TRACE=1 LINX_TLB_FILL_TRACE_VA_LO=0x3f7fa8d000 LINX_TLB_FILL_TRACE_VA_HI=0x3f7fa8dfff LINX_TLB_FILL_TRACE_COUNT_LO=6000000000 LINX_TLB_FILL_TRACE_COUNT_HI=7000000000 LINX_FAULT_TRACE=1 LINX_FAULT_TRACE_PC_LO=0x1556087f00 LINX_FAULT_TRACE_PC_HI=0x1556088100 python3 tools/spec2017/run_stage_qemu_matrix.py ... --bench 502.gcc_r --out-dir workloads/generated/specint-502-mprotect-tlbfill-20260630-r2`
  Evidence: `workloads/generated/specint-train-all-tlbfill-debug-qemu-20260630-r1/specint_fast_gate_summary.json`, `workloads/generated/specint-train-all-tlbfill-debug-qemu-20260630-r1/train-all/qemu_matrix_summary.json`, `workloads/generated/specint-train-all-tlbfill-debug-qemu-20260630-r1/train-all/initramfs/stage_b_summary.json`, `workloads/generated/specint-502-mprotect-tlbfill-20260630-r2/qemu_matrix_summary.json`, and `workloads/generated/specint-502-mprotect-tlbfill-20260630-r2/initramfs/502_gcc_r/run_001/qemu.log`.
  Result: `999.specrand_ir` passes strict train hash `0x973dcfc2`; `500.perlbench_r`, `505.mcf_r`, `520.omnetpp_r`, `523.xalancbmk_r`, `531.deepsjeng_r`, `541.leela_r`, and `557.xz_r` are heartbeat-backed live-timeout rows; `502.gcc_r` is a user-trap correctness row at `addr=0x3f7fa8d010`; `525.x264_r` is an initramfs VFS-root panic before useful heartbeat evidence. The focused 502 r2 summary includes `tlb_fill_trace_seen=true`, `tlb_fill_trace_count=1`, and a `LINX_TLB_FILL_TRACE` line with `ok=0`, `access=store`, `va=0x3f7fa8d010`, `legacy_why=type0`, and `legacy_desc=0x0`.
  Loop update: keep `LINX_QEMU_HEARTBEAT_INTERVAL=1000000000` in all-train diagnostics to distinguish live progress from deadlock by BPC/count churn. Use `LINX_TLB_FILL_TRACE_VA*` only in focused VM/page-fault runs. Current 502 owner is Linx Linux `mprotect()`/VMA/page-fault bring-up: log `do_mprotect_pkey()` VMA coverage and `do_page_fault()` `find_vma()`/`access_error()` for `0x3f7fa8d000..0x3f7fa8dfff` before changing QEMU TLB policy. Current live-timeout owners are QEMU/kernel throughput profiling; current 525 owner is initramfs/rootfs packaging or an alternate 9p/disk transport.

- [x] ID: SPEC-M01F Train-all gate shape exists and covers every current Linx SPECint rate benchmark.
  Static build command: `MODE=phase-b bash lib/musl/tools/linx/build_linx64_musl.sh && ./tools/build_linx_llvm_cpp_runtimes.sh --profile spec --mode phase-b && LINX_SPEC_FORCE_STATIC=1 bash tools/spec2017/build_int_rate_linx.sh --mode phase-b --force-static --emit-manifest workloads/generated/specint-build-after-oldmalloc-20260628/build_manifest_final.json`
  Static run command: `SPECINT_TRAIN_ALL_TIMEOUT=300 LINX_SPEC_HEARTBEAT_SEC=30 LINX_SPEC_QEMU_HEARTBEAT_INTERVAL=1000000000 LINX_SPEC_NO_PROGRESS_TIMEOUT=180 python3 tools/bringup/run_specint_fast_gate.py --profile train --out-dir workloads/generated/specint-train-all-20260628-after-oldmalloc --qemu emulator/qemu/build-linx/qemu-system-linx64 --append-extra norandmaps --guest-heartbeat-sec 0 --heartbeat-sec 30 --qemu-heartbeat-interval 1000000000 --no-progress-timeout 180 --continue-on-fail`
  Evidence: `workloads/generated/specint-build-after-oldmalloc-20260628/build_manifest_final.json`, `out/cpp-runtime/musl-cxx17-spec/summary_phase-b.json`, `avs/qemu/out/musl-static-oldmalloc-page-20260628/summary.json`, `workloads/generated/specint-train-all-20260628-after-oldmalloc/specint_fast_gate_summary.json`, `workloads/generated/specint-train-all-20260628-after-oldmalloc/train-all/qemu_matrix_summary.json`, and `workloads/generated/specint-train-all-20260628-after-oldmalloc/train-all/initramfs/stage_b_summary.json`.
  Status: suite wiring covers `500.perlbench_r`, `502.gcc_r`, `505.mcf_r`, `520.omnetpp_r`, `523.xalancbmk_r`, `525.x264_r`, `531.deepsjeng_r`, `541.leela_r`, `557.xz_r`, and `999.specrand_ir`.
  Static build result: all ten selected C/C++ benchmarks built as Linx executables; source immutability check passed.

- [x] ID: SPEC-M05-9P-TRAIN-ALL-FAST-20260630 9p train-all fast gate covers every SPECint row with timeout-as-failure.
  Run command: `python3 tools/spec2017/run_stage_qemu_matrix.py --qemu emulator/qemu/build-linx/qemu-system-linx64 --stage b --input-set train --transports 9p --sysroot out/libc/musl/install/phase-b --timeout 180 --heartbeat-sec 30 --qemu-heartbeat-interval 1000000000 --no-progress-timeout 90 --guest-heartbeat-sec 30 --append-extra 'norandmaps linx_storage_init=1' --dump-prefix-bytes 256 --stack-limit 2G --symbolize-heartbeat --fail-9p-timeout --out-dir workloads/generated/specint-train-all-9p-failtimeout-20260630-r1`
  Evidence: `workloads/generated/specint-525-9p-vmgetfeatures-wordfix-20260630-r1/525_x264_r/run_001/qemu.log` shows the 9p mount-tag feature reaching the guest (`LINX_VIRTIO_MMIO_FEATURES ... device=0x30000001`, selected features include bit 0) and both SPEC 9p mounts returning zero. `workloads/generated/specint-train-all-9p-failtimeout-20260630-r1/qemu_matrix_summary.json` and `workloads/generated/specint-train-all-9p-failtimeout-20260630-r1/9p/stage_b_summary.json` are the all-row fast-gate ledger.
  Result: the 9p transport/rootfs blocker for `525.x264_r` is closed for the SPEC init path. All ten train rows reach `LINX_SPEC_START`; none trap, panic, stall, or emit a wrapper child-exit marker. The fast gate is still `ok=false` because `--fail-9p-timeout` intentionally treats the 180s timeout as a failure to keep the all-row diagnostic bounded. Every row has `heartbeat_running=true`, `heartbeat_site_progress=true`, and increasing instruction counts, so the current 9p train failures are live throughput/gate-budget failures, not deadlocks.

  | Benchmark | 9p fast-gate status | Latest evidence | Proposed owner / next step |
  | --- | --- | --- | --- |
  | `500.perlbench_r` | `live-timeout` | count `22000000004`, BPC `0xffffffff801111a8`, `progress=site-change`, recent unique heartbeat sites `8` | Throughput/live-progress lane. Full initramfs correctness still has run_001 pass evidence; use 9p for large-input transport only after profiling its wait/path overhead. |
  | `502.gcc_r` | `live-timeout` | count `22000000002`, BPC `0xffffffff801052b2`, `progress=site-change`, no trap | Throughput/live-progress lane; correctness traps remain closed. |
  | `505.mcf_r` | `live-timeout` | count `22000000001`, BPC `0xffffffff801110b8`, `progress=site-change` | Throughput/live-progress lane. |
  | `520.omnetpp_r` | `live-timeout` | count `22000000006`, BPC `0xffffffff801052b2`, `progress=site-change`; no `sig=9` wrapper marker in this 9p fast gate | Reclassify the 9p path as live-slow first. Keep the older initramfs `sig=9` evidence in the resource lane until a current 9p or disk-transport full-output run reproduces it. |
  | `523.xalancbmk_r` | `live-timeout` | count `22000000001`, BPC `0xffffffff8010b8f8`, `progress=site-change`; no wrapper child exit | Same as 520: current 9p failure is live-slow, older child-exit evidence is transport/run-shape specific until reproduced. |
  | `525.x264_r` | `live-timeout` | count `22000000000`, BPC `0xffffffff80110b8e`, `LINX_SPEC_START` present; no VFS root panic and no 9p mount failure | Transport/rootfs blocker closed for SPEC 9p. Remaining owner is throughput/live progress and output validation. |
  | `531.deepsjeng_r` | `live-timeout` | count `20000000012`, BPC `0xffffffff801110d2`, `progress=site-change` | Throughput/live-progress lane. |
  | `541.leela_r` | `live-timeout` | count `22000000008`, BPC `0xffffffff801128d6`, `progress=site-change`; no wrapper child exit | Current 9p owner is live-slow; retain old initramfs child-exit evidence as historical/resource-lane evidence. |
  | `557.xz_r` | `live-timeout` | count `22000000005`, BPC `0xffffffff80110b5c`, `progress=site-change` | Throughput/live-progress lane. |
  | `999.specrand_ir` | `live-timeout` under 9p fast gate | count `22000000011`, BPC `0xffffffff80105196`, `progress=site-change`; initramfs strict hash pass remains `0x973dcfc2` | Use initramfs as the cheap correctness sentinel; use 9p specrand as a transport/profiling sentinel until 9p overhead is reduced. |

  Loop update: use `--fail-9p-timeout` only for fast all-row classification. Full 9p correctness/output validation should leave the default 9p timeout behavior in place, or run with a much larger timeout, because benchmark completion can be followed by PID1 exit/panic and host-visible specdiff is the authoritative 9p result. Use `linx_virtio_debug=1`, `LINX_VIRTIO_MMIO_DEBUG=1`, and `LINX_SYSCALL_TRACE_DUMP_ARGS=0,1,2,4` only for focused transport regressions; disable those noisy traces for suite runs. For throughput profiling, first run a focused 9p `999.specrand_ir` and one representative live row with guest heartbeat disabled and QEMU heartbeat coarse/off to separate wrapper polling and 9p path overhead from benchmark execution. When host samples include `helper_linx_tlb_iall`, rerun with `LINX_TLB_TRACE=1` plus `LINX_TLB_TRACE_COUNT_LO=<post-start-count>` before treating TLB invalidation as a SPEC steady-state bottleneck.

- [x] ID: SPEC-M05-LATEST-QEMU-TRAIN-ALL-STATIC-20260630 Latest static train-all failure ledger is current on rebuilt QEMU.
  Build command: `MODE=phase-b LINX_SPEC_FORCE_STATIC=1 bash tools/spec2017/build_int_rate_linx.sh --mode phase-b --force-static --emit-manifest workloads/generated/specint-build-all-static-byval-fix-20260630/phaseb_build_manifest.json --jobs 10`
  Run command: `python3 tools/spec2017/run_stage_qemu_matrix.py --qemu emulator/qemu/build-linx/qemu-system-linx64 --stage b --input-set train --transports initramfs --sysroot out/libc/musl/install/phase-b --timeout 300 --heartbeat-sec 30 --qemu-heartbeat-interval 1000000000 --no-progress-timeout 180 --guest-heartbeat-sec 0 --append-extra norandmaps --dump-prefix-bytes 256 --stack-limit 2G --symbolize-heartbeat --out-dir workloads/generated/specint-train-all-latest-qemu-20260630-r1`
  Evidence: QEMU `v10.2.0-970-g8dd1dcdbde2`, compiler/llvm commit `870fb448edd6f`, `workloads/generated/specint-build-all-static-byval-fix-20260630/phaseb_build_manifest.json`, `workloads/generated/specint-999-latest-qemu-heartbeat-smoke-20260630-r1/qemu_matrix_summary.json`, `workloads/generated/specint-train-all-latest-qemu-20260630-r1/qemu_matrix_summary.json`, and `workloads/generated/specint-train-all-latest-qemu-20260630-r1/initramfs/stage_b_summary.json`.
  Result: all supported Linx SPECint C/C++ train workloads build as static Linx executables; `548.exchange2_r` remains intentionally excluded as Fortran. The former `500.perlbench_r` run_002 bad branch target is closed by the LLVM Blockify ABI-call-argument fix, and the later `502.gcc_r addr=0x8` trap is closed by the LLVM by-value aggregate copy fix. The train matrix is still `ok=false`, but heartbeat/BPC evidence separates active execution from deadlock: every failed row has `heartbeat_running=true`, `heartbeat_site_progress=true`, and increasing instruction counts. `525.x264_r` is classified directly as `kernel-panic-loop-timeout` because symbolized heartbeat sites land in `panic.c`/`udelay`.

  | Benchmark | Status | Latest evidence | Proposed owner / next step |
  | --- | --- | --- | --- |
  | `500.perlbench_r` | run_001 pass; run_002 `live-timeout` | `perfect.b.3.out` hash `0xc69c7085` passes; run_002 reaches count `73000000000`, BPC `0x15556fc9cc`, `progress=site-change`, no trap | Correctness stop closed. Keep run_002 in the QEMU throughput lane; use longer budget only after hot-helper reductions. |
  | `502.gcc_r` | `live-timeout` | count `42000000004`, BPC `0xffffffff803def70`, `progress=site-change`, no `LINX_USER_TRAP`; focused byval-fix run reached count `61000000006` | Correctness stop closed. Keep the compiler regression for byval aggregate copies and move 502 into the QEMU throughput/live-progress lane; if kernel heartbeat sites dominate, symbolize the `vsprintf`/`string` breadcrumb before adding full traces. |
  | `505.mcf_r` | `live-timeout` | count `82000000003`, BPC `0x155555c8b4`, `progress=site-change` | Throughput/live-progress lane. Profile with heartbeat off or coarse. |
  | `520.omnetpp_r` | `spec-wrapper-fail` | child exits with `sig=9`; wrapper emits `LINX_SPEC_FAIL child-exit`; last heartbeat count `44000000005`, BPC `0xffffffff800b300e`; verified output file is empty; focused resource probes show 4 GiB is the largest current SPEC userspace memory lane and 4352 MiB+ panics before userspace | Wrapper/output plus resource lane. Capture child stderr/output and kernel kill reason first; do not classify as C++ startup. Keep >4 GiB guest memory in the separate Linx Linux high-memory panic lane. |
  | `523.xalancbmk_r` | `spec-wrapper-fail` | child exits with `sig=9`; wrapper emits `LINX_SPEC_FAIL child-exit`; last heartbeat count `46000000000`, BPC `0xffffffff800fb7a0`; verified output file is empty | Same wrapper/output lane as 520; inspect child exit and generated output before reopening stack/C++ correctness. |
  | `525.x264_r` | initramfs rootfs panic; 9p mount blocked | initramfs count `43000000002`, BPC `0xffffffff803e88aa`, recent heartbeat sites resolve to `panic.c`/`udelay`, no `LINX_SPEC_START`; focused PC-watch stops at `panic()` with caller in `init/do_mounts.c`; 9p reaches `LINX_SPEC_START` but `mount("spec2017", "/spec", "9p", ...)` returns `raw_rc=-14` | Transport/rootfs lane. Avoid giant 1.6 GiB initramfs for 525 train once 9p or a disk-image transport is fixed; Linux currently returns `-EFAULT` for the SPEC 9p mount even though QEMU arg-dump can read the options string. |
  | `531.deepsjeng_r` | `live-timeout` | count `74000000003`, BPC `0x15555683de`, `progress=site-change` | Throughput/live-progress lane; compare with the existing passing test-input profile. |
  | `541.leela_r` | `spec-wrapper-fail` | child exits with `sig=9`; wrapper emits `LINX_SPEC_FAIL child-exit`; last heartbeat count `44000000000`, BPC `0xffffffff800f2c0e`; verified output file is empty | Wrapper/output lane under `--stack-limit 2G`; collect child status/stderr and kernel kill reason before treating as stack recurrence. |
  | `557.xz_r` | `live-timeout` | count `73000000007`, BPC `0x1555577b92`, `progress=site-change` | Throughput/live-progress lane. |
  | `999.specrand_ir` | pass | strict hash `0x973dcfc2`; `LINX_SPEC_PASS 999.specrand_ir` | Keep as the cheap QEMU debug sentinel. |

  Loop update: rebuild `emulator/qemu/build-linx/qemu-system-linx64` before latest-ledger runs and confirm the binary matches `git -C emulator/qemu describe`. `tools/spec2017/run_int_rate_qemu.py` includes the wrapper `LINX_SPEC_DBG wait ... status/code/sig` line in `spec-wrapper-fail` evidence when the failure is `child-exit`, includes raw 9p mount return evidence in `chdir-rundir` failures, and classifies `LINX_DEBUG_PC_WATCH_EXIT=1` stops as `pc-watch-exit` instead of `none`. `--symbolize-heartbeat` now promotes 525 from raw live timeout to `kernel-panic-loop-timeout`. For first-panic captures, combine `LINX_DEBUG_PC_WATCH=<panic/vpanic pcs>`, `LINX_DEBUG_PC_WATCH_EXIT=1`, `LINX_CALL_TRACE_RING=1`, and `LINX_DEBUG_PC_WATCH_DUMP_CALL_RING=1` so the log records the caller ring before the panic delay loop dominates. For late fault/ring runs, combine `LINX_FAULT_TRACE_PC_LO/HI` with `LINX_FAULT_TRACE_COUNT_LO/HI`; for late stack/heap producer traces, combine `LINX_MEM_TRACE_ADDR/SIZE/ACCESS/ACR` with `LINX_MEM_TRACE_COUNT_LO/HI` so trace quotas survive to the final SPEC window.

- [x] ID: SPEC-M05-HIGHMEM-PANIC-20260630 >4 GiB guest-memory sweeps are a Linx Linux high-memory panic lane, not SPEC userspace progress.
  Debug command: `env LINX_DEBUG_PC_WATCH=0xffffffff80001574,0xffffffff8000159e LINX_DEBUG_PC_WATCH_REGS=1 LINX_DEBUG_PC_WATCH_DUMP_CODE_BYTES=32 LINX_DEBUG_PC_WATCH_EXIT=1 LINX_DEBUG_PC_WATCH_DUMP_CALL_RING=1 LINX_CALL_TRACE_RING=1 LINX_CALL_TRACE_RING_SIZE=128 python3 tools/spec2017/run_int_rate_qemu.py --bench 999.specrand_ir --input-set train --transport initramfs --memory-mb 8192 --stack-limit unlimited ...`
  Evidence: `workloads/generated/specint-999-mem4g-currentqemu-baseline-20260630-r1/` passes strict `999.specrand_ir` at 4096 MiB. `workloads/generated/specint-999-mem4352m-panic-threshold-20260630-r1/`, `workloads/generated/specint-999-mem5g-panic-threshold-20260630-r1/`, and `workloads/generated/specint-999-mem8g-panic-pcwatch-20260630-r1/` fail before `LINX_SPEC_START` with a PC-watch stop at `panic()`. The panic argument points at the kernel string `corrupted stack end detected inside scheduler`, and the QEMU call trace ring shows `schedule()` calling `panic()` before the delay loop.
  Result: the failure is not a global QEMU deadlock: heartbeat/BPC advances until the panic path, and the PC-watch stop captures the first cause. A stack-size-only kernel experiment is rejected because 32 KiB and 64 KiB init-stack rebuilds still reproduced the same scheduler stack-end panic; the source was reverted and the baseline 16 KiB `vmlinux` was rebuilt.
  Loop update: keep SPEC C++ resource sweeps at <=4096 MiB until the Linx Linux high-memory path is fixed. The next owner should inspect Linx memory topology/page-table/fixmap or boot-stack aliasing as guest RAM crosses 4 GiB, then rerun 4352 MiB `999.specrand_ir` as the sentinel before using larger memory to mask SPEC C++ RSS pressure.

- [x] ID: SPEC-M05-LATEST-TRAIN-ALL-20260629 Historical queue-inline train-all ledger is retained for comparison.
  Build command: `MODE=phase-b LINX_SPEC_FORCE_STATIC=1 bash tools/spec2017/build_int_rate_linx.sh --mode phase-b --force-static --emit-manifest workloads/generated/specint-train-all-icall-varfn-fix-20260629-r1/build_manifest.json`
  Run command: `LINX_SPEC_DUMP_PREFIX_BYTES=256 python3 tools/spec2017/run_stage_qemu_matrix.py --qemu emulator/qemu/build-linx/qemu-system-linx64 --stage b --input-set train --transports initramfs --sysroot out/libc/musl/install/phase-b --timeout 180 --heartbeat-sec 30 --qemu-heartbeat-interval 1000000000 --no-progress-timeout 180 --guest-heartbeat-sec 0 --append-extra norandmaps --dump-prefix-bytes 256 --stack-limit 2G --out-dir workloads/generated/specint-train-all-queue-inline-hbguard-20260629-r1`
  Evidence: `workloads/generated/specint-train-all-icall-varfn-fix-20260629-r1/build_manifest.json`, `avs/qemu/out/mremap-end-smoke-r3/summary.json`, `workloads/generated/specint-502-mremap-fix-20260629-r1/qemu_matrix_summary.json`, `workloads/generated/specint-train-all-queue-inline-hbguard-20260629-r1/initramfs/stage_b_summary.json`, `workloads/generated/specint-profile-500-queue-inline-20260629-r2/profile/qemu-500-queue-inline.sample.txt`, `workloads/generated/specint-profile-500-queue-inline-hbguard-20260629-r1/profile/qemu-500-queue-inline-hbguard.sample.txt`, `workloads/generated/specint-999-queue-inline-heartbeat-guard-20260629-r1/qemu_matrix_summary.json`, `workloads/generated/specint-999-queue-inline-noheartbeat-guard-20260629-r1/qemu_matrix_summary.json`, and `workloads/generated/specint-999-final-noheartbeat-strict-20260629-r1/qemu_matrix_summary.json`. Compiler-side regression evidence includes `compiler/llvm/llvm/test/CodeGen/LinxISA/f64-extload-constant.ll`, `compiler/llvm/llvm/test/CodeGen/LinxISA/icall-target-not-argreg.ll`, `avs/compiler/linx-llvm/tests/check_fp_extloads.py`, and `workloads/generated/avs-compiler-f64-extload-20260629-r1/`.
  Result: `ok=false`, elapsed `1652.321s`, stack limit `2G`, QEMU heartbeat interval `1000000000`; `999.specrand_ir` passes strict hash `0x973dcfc2`. Every failed benchmark has `heartbeat_running=true` and `heartbeat_site_progress=true`, so the current failures are not global QEMU deadlocks. The QEMU queue fast path removes `helper_linx_scalar_read_reg`, `helper_linx_tq_push`, and `helper_linx_uq_push` from the focused `500.perlbench_r` profile; the heartbeat guard removes `helper_linx_heartbeat` from the same profile. The remaining hot frames are tile/template/BSTART/probe work. The stage summary is the authoritative ledger for this multi-run train result; `run_stage_qemu_matrix.py` now aggregates the first failing subrun so a later `500.perlbench_r` failure is not hidden by a passing first subrun.

  | Benchmark | Status | Latest evidence | Proposed owner / next step |
  | --- | --- | --- | --- |
  | `500.perlbench_r` | first train subrun passes, second subrun `user-trap` | `perfect.b.3.out` hash `0xc69c7085` passes in run_001; run_002 traps at `addr=tpc=bpc=0x003f7fee56880000`, `orig_tpc=0x1555837c38`, `orig_bpc=0x1555837c18`, last heartbeat count `4000000025`, BPC `0x1555677c50` | BigInt range bug is closed. Current owner is branch/syscall-return correctness around musl `__syscall_cp_c` / `sccp`; use fault regs, call-trace ring, and PC-watch around `0x1555837c18..0x1555837c38` before changing control-flow rules. |
  | `502.gcc_r` | `live-timeout` | count `23000000004`, BPC `0x1555eb8e1e`, `progress=site-change` | Bad RTL/function-pointer and allocator/VM `mremap` lanes are closed. Current owner is throughput/live-progress profiling. |
  | `505.mcf_r` | `live-timeout` | count `46000000001`, BPC `0x155555c470`, `progress=site-change` | Treat current train input as throughput/live-progress. Reproduce older user traps before moving it back to correctness. |
  | `520.omnetpp_r` | `live-timeout` | count `30000000002`, BPC `0xffffffff803dd7f6`, `progress=site-change` | Null constructor/callback trap remains closed. Current owner is throughput/live-progress profiling. |
  | `523.xalancbmk_r` | `live-timeout` with `--stack-limit 2G` | count `41000000002`, BPC `0xffffffff803dd766`, `progress=same-site`, `heartbeat_site_progress=true`, no user trap | Stack-2G reclassifies the old stack-edge trap to live-slow. Keep 2G in train loops; profile before debugging C++ atomics. |
  | `525.x264_r` | `live-timeout` | count `24000000005`, BPC `0xffffffff803e88b0`, `progress=site-change`, no panic | Current owner is throughput/live progress; the older panic is historical unless it reappears. |
  | `531.deepsjeng_r` | `live-timeout` | count `44000000002`, BPC `0x15555607a4`, `progress=site-change` | Current train input is slow/live. Use a focused profile and compare against the earlier test-input pass profile to find remaining QEMU hot helpers. |
  | `541.leela_r` | `live-timeout` with `--stack-limit 2G` | count `39000000001`, BPC `0xffffffff803dd7f6`, `progress=site-change`, no user trap | Run train loops with `--stack-limit 2G`; remaining blocker is live-slow/profiling unless a larger input exposes a new correctness trap. |
  | `557.xz_r` | `live-timeout` | count `35000000002`, BPC `0x155558d612`, `progress=site-change`, no user trap | Current train input is slow/live. Profile before pursuing older bad-pointer evidence. |
  | `999.specrand_ir` | pass | strict hash `0x973dcfc2`; `LINX_SPEC_PASS 999.specrand_ir` in the current train-all run | Keep as the cheap QEMU debug sentinel. |

- [x] ID: SPEC-MATRIX-AGG-001 Matrix summary reports the first failing subrun for multi-command benchmarks.
  Resolution: `tools/spec2017/run_stage_qemu_matrix.py` now selects the first non-`none` QEMU subrun failure when aggregating `failure_classes` and `failure_details`, instead of always reporting subrun 1.
  Evidence: `python3 tools/spec2017/test_run_stage_qemu_matrix.py` covers a synthetic `500.perlbench_r` case where run_001 is clean and run_002 is `user-trap`; direct helper verification on `workloads/generated/specint-train-all-queue-inline-hbguard-20260629-r1/initramfs/stage_b_summary.json` reports `500.perlbench_r: user-trap` with log `run_002/qemu.log`.

- [x] ID: SPEC-M05-STACKLIMIT-SYSCALL SPEC wrapper stack-limit control bypasses the libc `setrlimit()` return bug.
  Resolution: generated init wrappers use a raw Linx `prlimit64` syscall for `RLIMIT_STACK` before falling back to libc `setrlimit()`. `LINX_SPEC_STACK_LIMIT_BYTES=<n>` overrides the default 256 MiB value; `LINX_SPEC_STACK_LIMIT_BYTES=unlimited` preserves the old unlimited-stack reproduction mode. The runner stack now also accepts size suffixes and the suite chain forwards `--stack-limit` through `run_specint_fast_gate.py`, `run_stage_qemu_matrix.py`, and `run_int_rate_qemu.py`.
  Evidence: `workloads/generated/specint-999-prlimit-trace-20260629-r1/initramfs/999_specrand_ir/run_001/qemu.log` shows syscall `261` returning `0` while libc `setrlimit()` still warns `errno=21`. `workloads/generated/specint-999-raw-prlimit-20260629-r1/initramfs/999_specrand_ir/run_001/qemu.log` shows syscall `261` returning `0`, `LINX_SPEC_DBG stack-limit=268435456`, no `setrlimit-stack` warning, and strict hash pass. `workloads/generated/specint-502-raw-prlimit-20260629-r1/initramfs/502_gcc_r/run_001/qemu.log` also logs `stack-limit=268435456`. `workloads/generated/specint-541-stack-unlimited-20260629-r1/541_leela_r/run_001/qemu.log` logs `stack-limit=unlimited`, has 802 QEMU heartbeats, reaches count `40050000003`, and has no `LINX_USER_TRAP`; the last observed user stack `0x3f8b358b60` is about `0x64ca74a0` bytes below `0x3ff0000000`. The focused suffix/CLI sweep records `512M` as `536870912`, `1G` as `1073741824`, and `2G` as `2147483648` in `stack_limit_defines` under `workloads/generated/specint-541-stack-{512m,1g,2g}-20260629-r1/`.
  Follow-up: keep a separate libc task for why musl `setrlimit()` reports `errno=21` after a successful `prlimit64`; the SPEC gate no longer depends on that wrapper for stack layout control.

- [x] ID: SPEC-M05-MATRIX-MEMORY SPEC matrix and fast gate can vary guest memory.
  Resolution: `run_stage_qemu_matrix.py` now accepts `--memory-mb` / `LINX_SPEC_MEMORY_MB`, forwards it to `run_int_rate_qemu.py`, and records `memory_mb` in JSON/Markdown summaries. `run_specint_fast_gate.py` accepts `--memory-mb` / `SPEC_MEMORY_MB` / `LINX_SPEC_MEMORY_MB`, validates matrix support, forwards the knob, and records it in fast-gate summaries.
  Evidence: `python3 -m py_compile tools/spec2017/run_stage_qemu_matrix.py tools/bringup/run_specint_fast_gate.py`, `python3 tools/spec2017/test_run_stage_qemu_matrix.py`, `python3 tools/bringup/run_specint_fast_gate.py --profile smoke --memory-mb 4096 --stack-limit unlimited --dry-run --out-dir workloads/generated/specint-fastgate-memory-dryrun-20260630-r1`, and `workloads/generated/specint-fastgate-memory-dryrun-20260630-r1/specint_fast_gate_summary.json` show `memory_mb=4096` and command forwarding of `--memory-mb 4096`.
  Follow-up: use this only for focused memory/resource sweeps until the >4 GiB kernel panic lane is understood.

- [x] ID: SPEC-M05-MALLOCNG-DEFAULT Phase-b musl now defaults to mallocng for SPEC/runtime bring-up.
  Resolution: `lib/musl/tools/linx/build_linx64_musl.sh` selects `MALLOC_IMPL=mallocng` by default while preserving `MALLOC_IMPL=oldmalloc` for targeted allocator bisection.
  Evidence: `out/libc/musl/logs/phase-b-summary.txt` records `malloc_impl=mallocng`; `avs/qemu/out/musl-static-mallocng-default-20260629/summary.json` passes focused static musl smoke for `malloc_printf`, `cpp17_smoke`, and `time_syscalls`; `out/cpp-runtime/musl-cxx17-spec/summary_phase-b.json` records the refreshed spec C++ runtime overlay; `workloads/generated/specint-build-mallocng-cxx-refresh-20260629-r1/build_manifest.json` rebuilds all ten selected INT C/C++ benchmarks with source immutability preserved; `workloads/generated/specint-train-all-20260629-mallocng-cxx-refresh-r1/train-all/initramfs/stage_b_summary.json` is the refreshed all-train QEMU ledger.
  Caveat: this is not a SPEC correctness fix. The mallocng refresh did not close the then-current `500.perlbench_r` arithmetic-range failure or the `502.gcc_r` trap. The later f64 extload compiler fix moved `500` to live-timeout while `502` still trapped at `addr=0x305910060a11b059`; the newer ICALL/502-workaround run superseded that 502 stop with the allocator/VM trap at `addr=0x3f7e728ffc`, which is now closed by the Linx Linux mremap workaround and `mremap_end` AVS smoke. Use oldmalloc only for reproducing older allocator-overlap evidence, not as the default phase-b runtime baseline.

- [x] ID: SPEC-M05-LATEST-TRAIN-ALL-20260628 Latest train-all failure ledger is current.
  Command: `LINX_QEMU_HEARTBEAT_CODE_BYTES=0 SPECINT_TRAIN_ALL_TIMEOUT=180 SPEC_GUEST_HEARTBEAT_SEC=0 SPEC_QEMU_HEARTBEAT_INTERVAL=50000000 SPEC_NO_PROGRESS_TIMEOUT=120 python3 tools/bringup/run_specint_fast_gate.py --profile train --spec-dir workloads/spec2017/cpu2017v118_x64_gcc12_avx2 --qemu emulator/qemu/build-linx/qemu-system-linx64 --sysroot out/libc/musl/install/phase-b --out-dir workloads/generated/specint-train-all-20260628-qemu-dump-regs-r1 --append-extra norandmaps --continue-on-fail --heartbeat-sec 30 --qemu-heartbeat-interval 50000000 --guest-heartbeat-sec 0 --no-progress-timeout 120`
  Evidence: `workloads/generated/specint-train-all-20260628-qemu-dump-regs-r1/specint_fast_gate_summary.json`, `workloads/generated/specint-train-all-20260628-qemu-dump-regs-r1/train-all/qemu_matrix_summary.json`, and `workloads/generated/specint-train-all-20260628-qemu-dump-regs-r1/train-all/initramfs/stage_b_summary.json`.
  Result: `ok=false`, elapsed `729.231s`; no failed benchmark was classified as `stalled`, and every failed benchmark had `heartbeat_running=true` plus `heartbeat_site_progress=true`, so the current failures are not global QEMU deadlocks.

  | Benchmark | Status | Latest evidence | Proposed owner / next step |
  | --- | --- | --- | --- |
  | `500.perlbench_r` | `user-arithmetic-range` | `Range iterator outside integer range at lib/Math/BigInt.pm line 2675`, count `3350000001`, BPC `0x15556ec224` | Minimize a Linx Perl scalar/range smoke and compare compiler/QEMU scalar IV/NV flag behavior before changing SPEC packaging. |
  | `502.gcc_r` | `user-trap` | `addr=0x10`, user `tpc=0x1556074e88`, user `bpc=0x1556074e80`, count `5650000001`, BPC `0xffffffff801f346c`; focused traces show `brk(0x1556273000) -> 0x1556273000`, later `brk(0x1556277000) -> 0x1556273000`, fallback `mmap(0, 0x1ba000, ...) -> 0x1556273000`, and overlapping chunks at `0x1556276010/0x1556276030` | Historical: EBADF is resolved by the QEMU predicated CALL skip fix, and the later allocator/VM mremap trap is now closed. Current 502 owner is the live-slow profiling lane unless a fresh run reproduces this class. |
  | `505.mcf_r` | `user-trap` | `addr=0x19`, user `tpc=0x155555b860`, user `bpc=0x155555b85a`, count `1300000010`, BPC `0xffffffff800fb0ba` | Treat as a correctness trap again, not throughput. Use fault-regs plus code-byte PC-watch around `primal_net_simplex`/stdio closeout and compare the open-file/list state before the final bad pointer. |
  | `520.omnetpp_r` | `user-trap` | null address trap at `tpc=0xeaea2`, `bpc=0xeae90`, count `750000003`, BPC `0xffffffff803dde02` | Continue as C++ object/callback correctness, not liveness; symbolize the section/config path and inspect static C++ ABI/state. |
  | `523.xalancbmk_r` | `live-timeout` | count `27850000004`, BPC `0xffffffff80090c62`, recent unique heartbeat sites `6`, `stalled=false` | Profile with heartbeat off/coarse, then target BSTART decode/cache and hot helper overhead; also symbolize the recurring kernel breadcrumb. |
  | `525.x264_r` | `live-timeout` | count `24050000000`, BPC `0xffffffff803e8f46`, recent unique heartbeat sites `4`, `stalled=false` | Latest run no longer reproduces the prior early panic; treat current blocker as throughput/live progress first, while keeping the older panic caller as historical evidence if it returns. |
  | `531.deepsjeng_r` | `user-trap` | branch-target trap with `tpc=0`, `bpc=0`, `bpcn=0x1555576390`, count `6700000005`, BPC `0xffffffff800fc628` | Focus branch-target legality/call-ret state with fault regs, call-trace ring, and PC-watch code bytes around the symbolized user path. |
  | `541.leela_r` | `live-timeout` | count `11850000002`, BPC `0xffffffff80090e84`, recent unique heartbeat sites `8`, `stalled=false` | Same performance lane as `523`/`525`; reduce QEMU overhead after correctness traps are separated. |
  | `557.xz_r` | `user-trap` | bad address `0x04000415794a241f`, user `tpc=0x155557fae8`, user `bpc=0x155557fada`, count `9050000000`, BPC `0xffffffff800fba2c` | Treat as pointer/control-flow corruption; symbolize failing block and compare compiler/QEMU branch and memory state. |
  | `999.specrand_ir` | pass | strict hash `0x973dcfc2`, count `450000004`, BPC `0xffffffff803dde92` | Keep as the cheap train pass sentinel for QEMU debug changes. |

- [x] ID: SPEC-QEMU-HB-001 BPC heartbeat switch exists.
  Switches: `LINX_HEARTBEAT_INTERVAL` or `LINX_QEMU_HEARTBEAT_INTERVAL`; fast-gate option `--qemu-heartbeat-interval`. Focused register snapshots use `LINX_HEARTBEAT_REGS=1` or `LINX_QEMU_HEARTBEAT_REGS=1`. Focused code-byte snapshots use `LINX_HEARTBEAT_CODE_BYTES=<n>` or `LINX_QEMU_HEARTBEAT_CODE_BYTES=<n>`. SPEC runner kernel heartbeat symbolization uses `--symbolize-heartbeat` or `LINX_SPEC_SYMBOLIZE_HEARTBEAT=1`.
  Done means: qemu logs emit `LINX_HEARTBEAT` with count, delta, PC, BPC, TPC, branch state, `progress=first|site-change|same-site`, `same_site`, TP/ETEMP breadcrumbs, stack/return registers, and selected argument registers. Optional companion records emit full GPRs or PC/BPC code bytes.
  Evidence: `workloads/generated/specint-heartbeat-smoke-20260628/test-smoke/initramfs/999_specrand_ir/run_001/qemu.log`, the rebuilt-QEMU train-all runs under `workloads/generated/specint-train-all-20260628-qemu-context-r1/train-all/initramfs/` and `workloads/generated/specint-train-all-20260628-qemu-dump-regs-r1/train-all/initramfs/`, `workloads/generated/specint-heartbeat-regs-smoke-20260628/initramfs/999_specrand_ir/run_001/qemu.log`, and `workloads/generated/specint-heartbeat-code-smoke-20260628-r1/initramfs/999_specrand_ir/run_001/qemu.log`.
  Result: the rebuilt-QEMU 2026-06-30 all-train run under `workloads/generated/specint-train-all-latest-qemu-20260630-r1/` marks no failed benchmark as `stalled`; every failed benchmark has `heartbeat_running=true` and `heartbeat_site_progress=true`, so the current failures are wrapper exits, live slow paths, or a symbolized kernel panic-loop rather than global QEMU deadlocks. The focused heartbeat-register smoke passes `999.specrand_ir` and emits `LINX_HEARTBEAT_REGS`; the focused heartbeat-code smoke passes `999.specrand_ir` and emits `LINX_HEARTBEAT_CODE`. The latest matrix resolves `525.x264_r` recent heartbeat sites to `panic.c` and `udelay` and classifies it as `kernel-panic-loop-timeout`.

- [x] ID: SPEC-QEMU-FCMP-DBG-001 Scalar FP compare tracing can classify range/arithmetic failures.
  QEMU commit: `b5e90c7db5f`.
  Switches: `LINX_FCMP_TRACE=1` or `LINX_FP_TRACE=1`, optional `LINX_FCMP_TRACE_PC_LO/HI`, `LINX_FCMP_TRACE_COUNT_LO/HI`, `LINX_FCMP_TRACE_LIMIT`, and `LINX_FCMP_TRACE_OP=feq,flt,fge`; the `LINX_FP_TRACE_*` aliases are accepted for the same filters.
  Done means: qemu logs emit bounded `LINX_FCMP_TRACE` records with op, instruction count, PC, BPC, TPC, source type, raw operands, interpreted `f64`/`f32` values, result, and FCSR.
  Evidence: `workloads/generated/specint-500-fcmp-trace-20260629-r2/qemu_matrix_summary.json` reproduces `500.perlbench_r` as `user-arithmetic-range` and now includes `fcmp_trace_seen=true`, `fcmp_trace_count=3`, and bounded operand samples. The per-benchmark `qemu.log` records `fge.fd`/`flt.fd` using raw operands `0xdf000000`, `0x0`, and `0x5f800000` against `1.0`. Static objdump confirms the producer is `hl.lwu.pcr` before `.fd` compares in `S_outside_integer`.
  Result: use this trace for future scalar FP/range failures before adding full instruction traces. This trace identified the Linx LLVM f64 constant materialization bug; after the backend fix and AVS/compiler regression, the latest `500.perlbench_r` train run no longer reports `user-arithmetic-range` and is now classified as a live timeout.

- [x] ID: SPEC-QEMU-TP-DBG-001 TP handoff tracing is available for focused SPEC and musl runs.
  Switches: `LINX_TP_TRACE=1`, optional `LINX_TP_TRACE_LIMIT`, `LINX_TP_TRACE_SSR=1`, and `LINX_TP_TRACE_READS=1`.
  Done means: qemu logs emit `LINX_TP_TRACE` records for service-request, sync-trap, IRQ, same-ACR frame setup, and ACRE handoff points, with user TP, kernel thread-info TP, ETEMP/ETEMP0 breadcrumbs, BPC/TPC, saved `x1` for same-ACR frames, and selected GPRs.
  Evidence: `avs/qemu/out/musl-tp-preserve-debug-r2-20260628/summary.json` passes and its qemu log includes `LINX_TP_TRACE event=service_user_to_kernel`; focused `523.xalancbmk_r` tracing under `workloads/generated/specint-523-tp-trace-20260628/run/` separated the old C++ startup TP issue from later stack-limit/throughput stops. Same-ACR frame events are implemented as `sync_same_acr_frame` and `irq_same_acr_frame` and are covered indirectly by the post-fix AVS/system and SPEC 500 runs.

- [x] ID: SPEC-QEMU-SYSCALL-DBG-001 Syscall trace can identify path/fd failures without full traces.
  Switches: `LINX_SYSCALL_TRACE=1`, optional `LINX_SYSCALL_TRACE_NR` as a single syscall number or comma-separated list, `LINX_SYSCALL_TRACE_PC_LO/HI`, `LINX_SYSCALL_TRACE_LIMIT`, `LINX_SYSCALL_TRACE_STRINGS=1`, `LINX_SYSCALL_TRACE_STRING_MAX`, `LINX_SYSCALL_TRACE_REGS=1`, and shared `LINX_TRACE_REGS=1`.
  Done means: qemu logs emit syscall entry/return pairs, entry arguments on returns, unpaired syscall markers, separate `LINX_SYSCALL_ARGSTR` records for pathname arguments, and opt-in `LINX_SYSCALL_REGS` full-GPR records.
  Evidence: `workloads/generated/specint-502-syscall-argstr-smoke-20260628/run/initramfs/502_gcc_r/run_001/qemu.log` contains `LINX_SYSCALL_ARGSTR` records for `/dev/console`, `/spec-run`, SPEC output/input paths, and `.linx_empty_stdin`. `workloads/generated/specint-531-test-filesys-trace-20260701-r1/531_deepsjeng_r/run_001/qemu.log` uses `LINX_SYSCALL_TRACE_NR=48,56,78,79,221,291` to keep exec/file-path tracing below the event budget and separate 531 input packaging from the current C++ runtime failure.

- [x] ID: SPEC-QEMU-FAULT-DBG-001 Fault trace can dump full Linx register state.
  Switches: `LINX_FAULT_TRACE=1`, `LINX_FAULT_TRACE_REGS=1`, optional `LINX_FAULT_TRACE_LIMIT`, `LINX_FAULT_TRACE_ADDR=<va>` / `_ADDR_LO/_ADDR_HI` including literal `0`, and shared `LINX_TRACE_REGS=1`.
  Done means: qemu logs emit `LINX_FAULT_REGS` after a synchronous fault with all GPRs, BPC/TPC context, and instruction count.
  Evidence: `workloads/generated/specint-cxx-after-oldmalloc-20260628/run/initramfs/520_omnetpp_r/run_001/qemu.log`, `workloads/generated/specint-cxx-after-oldmalloc-20260628/run/initramfs/523_xalancbmk_r/run_001/qemu.log`, and `workloads/generated/specint-cxx-after-oldmalloc-20260628/run/initramfs/541_leela_r/run_001/qemu.log`.

- [x] ID: SPEC-QEMU-FRET-STK-DBG-001 FRET.STK trace can prove restore-slot state.
  Switches: `LINX_FRET_STK_TRACE=1`, optional exact/range filters `LINX_FRET_STK_TRACE_PC=<pc>` / `_PC_LO/_PC_HI`, late-window filters `LINX_FRET_STK_TRACE_COUNT_LO/HI`, restored-RA filter `LINX_FRET_STK_TRACE_RA=<value>` including literal `0`, `LINX_FRET_STK_TRACE_LIMIT`, `LINX_FRET_STK_TRACE_DUMP_WORDS=<n>`, `LINX_FRET_STK_TRACE_REGS=1`, and shared `LINX_TRACE_REGS=1`. Matching `LINX_QEMU_FRET_STK_TRACE_*` aliases are accepted.
  Done means: qemu logs emit `LINX_FRET_STK_TRACE` plus one `LINX_FRET_STK_SLOT` per restored GPR before the restore is committed, including the computed stack address and loaded value.
  Evidence: added during SPEC 500 allocator triage after the call ring showed final `FRET.STK` restoring `ra=0`; use the focused 500 reproducer with `LINX_FRET_STK_TRACE_PC=0x1555828d20`, `LINX_FRET_STK_TRACE_RA=0`, and `LINX_FRET_STK_TRACE_DUMP_WORDS=16` to capture the failing frame.

- [x] ID: SPEC-QEMU-PCWATCH-DBG-001 PC watch can dump full Linx register state and deferred ring snapshots.
  Switches: `LINX_DEBUG_PC_WATCH=<pc>[,<pc>...]`, `LINX_DEBUG_PC_WATCH_REGS=1`, `LINX_DEBUG_PC_WATCH_DUMP_CODE_BYTES=<n>`, optional `LINX_DEBUG_PC_WATCH_EXIT=1`, count filters `LINX_DEBUG_PC_WATCH_COUNT_LO=<insns>` / `LINX_DEBUG_PC_WATCH_COUNT_HI=<insns>`, hit filters `LINX_DEBUG_PC_WATCH_HIT_LO=<n>` / `LINX_DEBUG_PC_WATCH_HIT_HI=<n>`, print cap `LINX_DEBUG_PC_WATCH_HIT_LIMIT=<n>`, GPR value match filter `LINX_DEBUG_PC_WATCH_MATCH_GPR=<gpr>` plus `LINX_DEBUG_PC_WATCH_MATCH_VALUE=<value>` and optional `LINX_DEBUG_PC_WATCH_MATCH_MASK=<mask>`, memory dump source `LINX_DEBUG_PC_WATCH_DUMP_REG=<gpr|tp|tqN|uqN|t#N|u#N>`, memory dump source list `LINX_DEBUG_PC_WATCH_DUMP_REGS=<reg>[,<reg>...]`, `LINX_DEBUG_PC_WATCH_DUMP_WORDS=<n>`, `LINX_DEBUG_PC_WATCH_DUMP_OFFSET=<bytes>`, multi-slot offset list `LINX_DEBUG_PC_WATCH_DUMP_OFFSETS=<bytes>[,<bytes>...]`, one-hop pointer-slot offset list `LINX_DEBUG_PC_WATCH_DUMP_PTR_OFFSETS=<bytes>[,<bytes>...]`, memory dump unit width `LINX_DEBUG_PC_WATCH_DUMP_WIDTH=1|2|4|8`, backward-compatible `LINX_DEBUG_PC_WATCH_DUMP_A0_WORDS=<n>` / `LINX_DEBUG_PC_WATCH_DUMP_A0_OFFSET=<bytes>`, `LINX_DEBUG_PC_WATCH_PRINT=0`, `LINX_DEBUG_PC_WATCH_RING=1`, `LINX_DEBUG_PC_WATCH_RING_SIZE=<1..128>`, ring memory source `LINX_DEBUG_PC_WATCH_RING_MEM_REG=<gpr|tp|tqN|uqN|t#N|u#N>`, ring memory offset `LINX_DEBUG_PC_WATCH_RING_MEM_OFFSET=<bytes>`, and shared `LINX_TRACE_REGS=1`.
  Done means: qemu logs emit `linx_pc_watch:` plus optional `LINX_PC_WATCH_REGS` with all GPRs, BPC/TPC context, instruction count at a focused PC, optional `LINX_PC_WATCH_CODE` bytes for symbolizing ambiguous runtime mappings, optional memory units from one or more selected register/queue pointers, and `printed=<n>` so noisy symbols can be capped without hiding total hits. The translator emits the host debug helper only for exact watched PCs, and count filters arm PC matching before the watch-list scan, making late SPEC windows practical; hit counts are local to the armed count window. Default memory dumps remain 8-byte words; `LINX_DEBUG_PC_WATCH_DUMP_WIDTH` exposes narrower 1/2/4-byte fields when source structures require it. `LINX_DEBUG_PC_WATCH_DUMP_PTR_OFFSETS` reads a 64-bit guest pointer from selected source offsets and dumps the pointee memory in the same hit. Ring-only mode records matching hits after filters pass and emits `LINX_PC_WATCH_RING` / `LINX_PC_WATCH_RING_ENTRY` records when `LINX_FAULT_TRACE` catches a synchronous fault; ring entries can also include a non-perturbing guest memory snapshot from a selected pointer source.
  Evidence: `workloads/generated/specint-pcwatch-regs-smoke-20260628-r1/initramfs/999_specrand_ir/run_001/qemu.log` contains `LINX_PC_WATCH_REGS` for watched PC `0xffffffff80404da6`; `workloads/generated/specint-pcwatch-filter-smoke-20260628-r1/999_specrand_ir/run_001/qemu.log` passes `999.specrand_ir` and contains a filtered `linx_pc_watch:` record with `printed=1 count=655930`; `workloads/generated/specint-pcwatch-dump-regs-smoke-20260628-r1/initramfs/999_specrand_ir/run_001/qemu.log` passes `999.specrand_ir` and proves `LINX_DEBUG_PC_WATCH_DUMP_REGS=sp,tp,a0` emits three same-hit memory dumps; `workloads/generated/specint-pcwatch-dump-offsets-smoke-20260629-r1/initramfs/999_specrand_ir/run_001/qemu.log` passes `999.specrand_ir` and proves `LINX_DEBUG_PC_WATCH_DUMP_OFFSETS=0,8` emits both `sp+0x0` and `sp+0x8` in one hit; `workloads/generated/specint-pcwatch-width-smoke-20260629-r2/initramfs/999_specrand_ir/run_001/qemu.log` passes strict `999.specrand_ir`, logs `width=4`, and preserves the expected hash `0x973dcfc2`; `workloads/generated/specint-pcwatch-ptr-offset-smoke-20260629-r2/initramfs/999_specrand_ir/run_001/qemu.log` passes strict `999.specrand_ir` and proves pointer-slot output with `sp+0x0->0x7ffff000`; `workloads/generated/specint-500-ppflop-width4-watch-20260629-r1/initramfs/500_perlbench_r/run_001/qemu.log` uses the width switch in the deterministic Perl range failure window; `workloads/generated/specint-500-ppflop-branch-ptrslots-20260629-r1/initramfs/500_perlbench_r/run_001/qemu.log` uses pointer-slot dumps in the optimized `pp_flop` window; `workloads/generated/specint-502-alloc-pcwatch-a2-20260628-r1/initramfs/502_gcc_r/run_001/qemu.log` proves `LINX_DEBUG_PC_WATCH_DUMP_REG=a2` dumps allocator chunk words; `workloads/generated/specint-502-alloc-pcwatch-match-a2-20260628-r1/initramfs/502_gcc_r/run_001/qemu.log` proves `MATCH_GPR=a2`/`MATCH_VALUE=0x1556276030` isolates the final allocator trap chunk; `workloads/generated/specint-500-pcwatch-ring-a1-20260629-r1/500_perlbench_r/run_001/qemu.log` proves ring-only PC-watch preserves the 500 kernel-panic path while dumping the final dentry compare state; `workloads/generated/specint-500-pcwatch-ring-mem-a1-20260629-r1/500_perlbench_r/run_001/qemu.log` proves ring memory snapshots can show `x1=0x1` while `[a1+0x20]` still contains `0xff60000004c02db8`.
  Latest fast-path evidence: `workloads/generated/specint-999-pcwatch-exact-qemu-fastpath-20260629-r1/initramfs/999_specrand_ir/run_001/qemu.log` passes strict `999.specrand_ir`, emits `linx_pc_watch:` and `LINX_PC_WATCH_REGS` for exact PC `0xffffffff80404da6`, and preserves hash `0x973dcfc2`; `workloads/generated/specint-502-pcwatch-ring-final-20260629-r1/initramfs/502_gcc_r/run_001/qemu.log` proves the exact-PC/ring path remains fast enough to reach the final 502 user trap and dump the decisive last four watched instructions.

- [x] ID: SPEC-QEMU-MEMTRACE-DBG-001 Memory trace can target loads or stores without instrumenting the opposite access class.
  Switches: `LINX_MEM_TRACE_ADDR=<addr>`, optional `LINX_MEM_TRACE_SIZE=<bytes>`, `LINX_MEM_TRACE_LIMIT=<n>`, `LINX_MEM_TRACE_PC_LO=<pc>`, `LINX_MEM_TRACE_PC_HI=<pc>`, `LINX_MEM_TRACE_ACCESS=loads|stores|all`, `LINX_MEM_TRACE_ACR=<0..15>`, `LINX_MEM_TRACE_CONTEXT=1`, and `LINX_MEM_TRACE_FAST=0` to disable the translated fast overlap guard.
  Done means: qemu logs emit `LINX_MEM_TRACE` for overlapping memory accesses, `LINX_MEM_TRACE_ACCESS=stores` emits only store helpers at translation time so long SPEC runs can trace heap metadata without load-helper overhead, `LINX_MEM_TRACE_ACR` filters to user or kernel ACR, and `LINX_MEM_TRACE_CONTEXT=1` appends `mmu_idx`, `ttbr0`, `ttbr1`, and `tcr`. With the default fast path, translated code skips the helper call unless an access overlaps the watched range.
  Evidence: `workloads/generated/specint-502-alloc-memtrace-store-20260628-r1/initramfs/502_gcc_r/run_001/qemu.log` captures stores to `0x1556276048`; `workloads/generated/specint-502-bin39-head-memtrace-20260628-r1/initramfs/502_gcc_r/run_001/qemu.log` captures bin 39 head writes; `workloads/generated/specint-502-chunk-next-memtrace-20260628-r1/initramfs/502_gcc_r/run_001/qemu.log` captures writes to chunk link `0x1556276020`; `workloads/generated/specint-502-free-overlap-memtrace-context-20260628-r1/initramfs/502_gcc_r/run_001/qemu.log` proves `LINX_MEM_TRACE_ACR=2` and `LINX_MEM_TRACE_CONTEXT=1` keep the overlapping free-list stores in one user address space; `workloads/generated/specint-502-memtrace-context-smoke-20260628-r2/initramfs/502_gcc_r/run_001/qemu.log` proves the rebuilt QEMU emits the new context fields; `workloads/generated/specint-500-dentry-name-storetrace-fast-20260629-r1/500_perlbench_r/run_001/qemu.log` proves the fast overlap guard still captures the dentry name pointer store with context.

- [x] ID: SPEC-QEMU-FALL-CALL-001 Predicated FALL skip handles adjacent CALL/ICALL blocks.
  Resolution: QEMU now recognizes an adjacent call-like BSTART while resolving a predicated FALL skip, uses the following SETRET as the return continuation, and follows a direct return-continuation header to the semantic join block when present. This prevents skipped conditional calls from entering the callee or call-return body with stale arguments.
  Evidence: `python3 avs/qemu/run_callret_contract.py` passes the new `cond_skips_adjacent_hl_call` regression; `bash avs/qemu/check_system_strict.sh` passes; `workloads/generated/specint-502-after-call-skip-join-20260628-r1/stage_b_summary.json` moves `502.gcc_r` from the prior `Bad file number` stop to a later user trap at musl malloc `unbin`; `workloads/generated/specint-train-all-20260628-call-skip-join-r1/train-all/initramfs/stage_b_summary.json` confirms the same train-all behavior.
  Rejected: skipping to the SETRET target alone still executes the call-return body; skipping to the callee body keeps the original `fstat` fallback path corrupted.

- [x] ID: SPEC-M05-SMOKE `999.specrand_ir` train input passes under the all-train run.
  Evidence: `workloads/generated/specint-train-all-20260629-same-acr-x1-r1/train-all/initramfs/stage_b_summary.json` records `ok=true`, last heartbeat count `450000000`, BPC `0xffffffff803dde02`, and strict FNV-1a hash `0x973dcfc2` for `rand.11.out`. Older all-train runs under `workloads/generated/specint-train-all-20260628-*` also contain `LINX_SPEC_PASS 999.specrand_ir` and remain historical smoke evidence.
  Note: a shared-runtime rebuild made `999.specrand_ir` a 15 KiB dynamic executable and it trapped in shared startup; the static phase-b executable is the current correctness gate until shared SPEC runtime is green.

- [x] ID: SPEC-M05-GTOD-502 Legacy `gettimeofday` no longer poisons 502 diagnostics.
  Resolution: Linx VDSO syscall fallbacks now load the syscall number in `a7`, and `sys_gettimeofday()` uses a Linx-local `copy_to_user()` copyout instead of the current faulting 64-bit `put_user()` path.
  Evidence: `workloads/generated/specint-502-static-gettimeofday-copyout-20260628/run/initramfs/502_gcc_r/run_001/qemu.log` shows syscall `169` returning `0` after the focused fix, and `avs/qemu/out/musl-time-syscalls-20260628/summary.json` passes the focused musl `time_syscalls` sample.
  Follow-up: the remaining `502.gcc_r` EBADF diagnostic is no longer explained by syscall `169`; the full post-fix trace contains no `-EBADF` syscall returns.

- [x] ID: SPEC-M05-EXECVE-500 `500.perlbench_r` static PIE is present and readable before `execve`.
  Resolution: the original `errno=2` classification was narrowed by the init-wrapper pre-exec probe; the benchmark path is valid in the initramfs.
  Evidence: `workloads/generated/specint-500-preexec-20260628/initramfs/500_perlbench_r/run_001/qemu.log` shows `stat=0`, `open=6`, `read4=4`, and ELF magic `0x7f454c46`.
  Follow-up: the prior `Math::BigInt` range failure is closed by the Linx LLVM f64 extload fix. The current all-train run reaches a later `500.perlbench_r` command: run_001 passes by hash, and run_002 traps at bad target `0x003f7fee56880000`; track that under the active 500 branch/syscall-return correctness lane.

- [x] ID: SPEC-M05-FIXUP-500 Linx Linux recognizes v0.56 faultable usercopy fixup blocks.
  Resolution: `arch/linx/mm/extable.c` now accepts nonzero-offset 32-bit and 48-bit `BSTART.{STD,SYS,FP} FALL<, fixup_label>` fixup encodings before the legacy 128-bit block-header fallback.
  Evidence: `workloads/generated/specint-500-fixup-20260628/initramfs/500_perlbench_r/run_001/qemu.log` no longer stops at the earlier `sys_fcntl` usercopy `HL.BSTART.STD FALL` Oops.
  Verification: `run_linux_vmlinux_build_clean.sh --target vmlinux` rebuilt `kernel/linux/build-linx-fixed/vmlinux`; focused 500 rerun advanced to a different Oops.

- [x] ID: SPEC-M05-KMALLOC-500 `500.perlbench_r` completes the earlier `filelock_cache` slab-cache path.
  Resolution: the Linx curated init path now initializes the file-lock slab cache, moving past the prior `kmem_cache_alloc_noprof` null-cache fault.
  Evidence: `workloads/generated/specint-500-after-filelock-20260628/`, `workloads/generated/specint-500-syscall-openat-ret-20260628/`, and `workloads/generated/specint-500-stdin-empty-20260628/`.

- [x] ID: SPEC-M05-DLOOKUP-500 `500.perlbench_r` must stop corrupting the VFS dentry/name lookup path.
  Resolution: the Oops was not dentry memory corruption. QEMU same-ACR trap/IRQ entry did not save interrupted `x1` into the current bank's ETEMP while clearing live `x1` to zero, which Linux's kernel-origin entry path expects. QEMU now prepares that same-ACR frame before vectoring.
  Evidence: `workloads/generated/specint-500-baseline-20260629-r1/stage_b_summary.json` reproduces the original `__d_lookup_rcu` Oops. `workloads/generated/specint-500-pcwatch-ring-mem-a1-20260629-r1/500_perlbench_r/run_001/qemu.log` shows the decisive state: the final ring entries have live `x1=0x1`, but `[a1+0x20]` still reads `0xff60000004c02db8`, so the dentry name field is valid. `workloads/generated/specint-500-after-same-acr-x1-20260629-r1/stage_b_summary.json` and `workloads/generated/specint-train-all-20260629-same-acr-x1-r1/train-all/initramfs/stage_b_summary.json` no longer panic and classify 500 as `user-arithmetic-range`.
  Follow-up: keep the same-ACR `x1` frame covered by AVS; reopen this item only if `__d_lookup_rcu` reproduces.

- [x] ID: SPEC-M05-BIGINT-500 Prior `500.perlbench_r` Perl BigInt range failure is closed.
  Resolution: after the same-ACR `x1` fix, this was the active 500 blocker; the Linx LLVM f64 extload constant-materialization fix closes it. The current `500.perlbench_r` blocker is the later train run_002 bad branch-target trap, not `Math::BigInt` range handling.
  Evidence: `workloads/generated/specint-train-all-20260629-pcwatch-offsets-r1/train-all/initramfs/stage_b_summary.json` classifies `500.perlbench_r` as `user-arithmetic-range` with `Range iterator outside integer range at lib/Math/BigInt.pm line 2675`; heartbeat was still running with site progress, count `3350000008`, and BPC `0x15556193a0`. The focused rerun `workloads/generated/specint-500-after-same-acr-x1-20260629-r1/stage_b_summary.json` reproduces the same class without a kernel panic. `workloads/generated/specint-500-ppflop-offsets-20260629-r1/initramfs/500_perlbench_r/run_001/qemu.log` proves `LINX_DEBUG_PC_WATCH_DUMP_OFFSETS` can capture multiple `pp_flop` stack/frame slots in the deterministic failure window while preserving the same child-exit class. `workloads/generated/specint-500-ppflop-sv-objects-20260629-r2/initramfs/500_perlbench_r/run_001/qemu.log` captures final `pp_flop` hits 423-425 with GPRs and heap-object words before the range exception. `workloads/generated/specint-500-ppflop-width4-watch-20260629-r1/initramfs/500_perlbench_r/run_001/qemu.log` adds 4-byte field dumps in the same window. `workloads/generated/specint-500-ppflop-branch-ptrslots-20260629-r1/initramfs/500_perlbench_r/run_001/qemu.log` proves pointer-slot dumps can capture one-hop SV/frame state; the final watched error-build block has `pc=0x1555829792`, `a2=0x2`, and pointee dumps such as `sp+0x30->0x155588a9b0`. `workloads/generated/specint-500-ppflop-branch-countwin-20260629-r1/initramfs/500_perlbench_r/run_001/qemu.log` removes per-PC hit filtering and records repeated `0x1555829792` then `0x15558297ae` pairs, with no watched `0x15558297d4` continuation in the final count window, before the same range exception. Older evidence in `workloads/generated/specint-train-all-20260628-qemu-dump-regs-r1/train-all/initramfs/stage_b_summary.json` and `workloads/generated/specint-500-bigint-debug-20260628-r1/stage_b_summary.json` shows the same BigInt failure before the dlookup regression. Host `/usr/bin/perl -I./lib perfect.pl b 3` in the same run directory exits 0 with the expected first three perfect numbers, so argv/input packaging is not the current owner.
  New 500-specific evidence: ignored temporary `Devel::Peek` instrumentation immediately before `for my $i (1 .. $count)` observed `$count` as `FLAGS = (IOK,pIOK)` with `IV = 2`, yet the range still raised the same exception. A Linx C microprobe for the `SVf_IOK|SVf_IVisUV` guard compiled correctly at `-O0` and `-O2`; the visible `andiw 32` in the optimized `pp_flop` window is therefore not a standalone IVisUV-mask codegen bug.
  Perturbation note: ignored SPEC-library instrumentation in `workloads/generated/specint-500-bigint-debug-20260628-r2/` shifted the source line, and replacing the range expression with an `eval` wrapper in `workloads/generated/specint-500-bigint-debug-20260628-r3/` changed the symptom to live timeout at 180s with heartbeat count `20450000004`, BPC `0x15556f4116`, and `stalled=false`. Focused QEMU PC-watch runs around Perl range/scalar symbols are also perturbing: `workloads/generated/specint-500-pcwatch-20260628-r1/stage_b_summary.json` failed before exec with `errno=2` despite the generated CPIO list containing the benchmark, and `workloads/generated/specint-500-pcwatch-20260628-r2/stage_b_summary.json` hit a live kernel Oops/`LINX_EXIT_INIT` at count `2250000011` before any watched user PC fired. Treat these as sensitivity evidence, not the canonical 500 root cause.
  Follow-up: keep the optimized `pp_flop` and FCMP trace evidence as historical root-cause material. Reopen this item only if a fresh fixed-compiler run reproduces the range exception; otherwise track current 500 work under the branch/syscall-return correctness lane around `orig_tpc=0x1555837c38`.

- [x] ID: SPEC-M05-USERTRAP-500-SCRABBL `500.perlbench_r` run_002 no longer jumps to the bad user target.
  Resolution: the bad `op_ppaddr` target was produced before QEMU dispatched it. Focused memory tracing showed `Perl_repeatcpy` copying `0x602` bytes where the call argument should have been 8 bytes; static disassembly showed the shifted byte-count producer for ABI register `a2` had been erased. `compiler/llvm` commit `82776bf21f5ec` keeps Blockify from folding away ABI transfer-register values that are still consumed by call/return/tail exits.
  Evidence: `workloads/generated/specint-500-run002-opppaddr-memtrace-20260629-r1/qemu.log` captures the corrupt store to the op dispatch slot, and the fixed static rebuild under `workloads/generated/specint-500-train-static-after-callarg-fix-20260629-r1/qemu_matrix_summary.json` moves run_002 from `user-trap` to heartbeat-backed `live-timeout`. The latest rebuilt-QEMU all-static train ledger under `workloads/generated/specint-train-all-latest-qemu-20260630-r1/` keeps run_001 hash pass `0xc69c7085` and run_002 live at count `73000000000`.
  Follow-up: keep `500.perlbench_r` in the live-slow throughput lane unless a fresh current run reproduces a trap.

- [x] ID: SPEC-M05-FD-502 `502.gcc_r` must read `200.c` correctly.
  Resolution: the prior `cpugcc_r_base.mytest-m64: fatal error: 200.c: Bad file number` stop was traced to QEMU skipping a predicated FALL into an adjacent conditional CALL shape incorrectly. The fixed skip target now follows the SETRET return continuation through its direct join block, so the musl `fstat` procfd fallback is not entered with stale arguments.
  Evidence: `workloads/generated/specint-502-fstat-argdump-20260628-r2/502_gcc_r/run_001/qemu.log` shows `newfstatat(3, "", stat=0x3ffffff758, AT_EMPTY_PATH) -> 0`, and `LINX_SYSCALL_ARGDUMP` decodes with `st_mode=0x81a4` at stat offset 16. `workloads/generated/specint-502-fstat-syscall-after-call-skip-body-20260628-r1/502_gcc_r/run_001/qemu.log` showed that skipping only to the call-return body still took the stale fallback path. `workloads/generated/specint-502-after-call-skip-join-20260628-r1/stage_b_summary.json` no longer reports `Bad file number`; it reaches a later user trap.
  Follow-up: track the remaining `502.gcc_r` failure under `SPEC-M05-MALLOC-502`.

- [x] ID: SPEC-M05-MALLOC-502 `502.gcc_r` must survive the post-fstat allocator path.
  Resolution: after the predicated CALL skip fix, stack-limit loop update, mallocng default, Linx LLVM f64 extload fix, indirect-call target-register fix, and 502-only variadic-function-table workaround, `502.gcc_r` moved to a musl `realloc`/`mremap` end-page trap at `addr=0x3f7e728ffc`. `avs/qemu/tests/linux_musl_mremap_end.c` reproduced the primitive failure: `mremap(old_len=0x21000,new_len=0x41000,MREMAP_MAYMOVE)` returned success and the first store at `new + new_len - 1` faulted. The Linx Linux fix avoids the unstable maple-tree merge path for `MREMAP_MAYMOVE` expansion and mremap copy, producing a complete destination VMA. The focused smoke `avs/qemu/out/mremap-end-smoke-r3/summary.json` now passes, and focused `502.gcc_r` moves to live timeout with no trap.
  Evidence: `workloads/generated/specint-502-after-call-skip-join-20260628-r1/stage_b_summary.json` and `workloads/generated/specint-train-all-20260628-call-skip-join-r1/train-all/initramfs/stage_b_summary.json` classify the benchmark as `user-trap`. With the static PIE slide `0x1515555000`, runtime `0x1556074e88` maps to static `0x40b1fe88` in musl oldmalloc `unbin`, at `c.sdi t#1, [u#1, 16]`. The targeted PC-watch run `workloads/generated/specint-502-alloc-pcwatch-match-a2-20260628-r1/initramfs/502_gcc_r/run_001/qemu.log` captures hit `809` immediately before the trap: `a2=0x1556276030`, chunk words `[2]=0x1556273fe0` and `[3]=0`, so oldmalloc is unlinking a bad free-list entry.
  New evidence: `workloads/generated/specint-502-free-overlap-memtrace-context-20260628-r1/initramfs/502_gcc_r/run_001/qemu.log` uses `LINX_MEM_TRACE_ACR=2` plus `LINX_MEM_TRACE_CONTEXT=1` to prove the older overlapping free chunks share one user address space. It shows oldmalloc first splitting/binning `0x1556276010`, then splitting/binning `0x1556276030`, then allocating payload at `0x1556276020` while `0x1556276030` remains linked as a free chunk. `workloads/generated/specint-502-brk-syscall-trace-20260628-r1/initramfs/502_gcc_r/run_001/qemu.log` shows `brk(0x1556273000) -> 0x1556273000`, later failed growths such as `brk(0x1556277000) -> 0x1556273000`, and the final trap. `workloads/generated/specint-502-mmap-syscall-trace-20260628-r1/initramfs/502_gcc_r/run_001/qemu.log` shows the fallback anonymous `mmap(0, 0x1ba000, PROT_READ|PROT_WRITE, MAP_PRIVATE|MAP_ANONYMOUS, -1, 0) -> 0x1556273000`, matching the stalled brk frontier. A rejected one-page kernel guard in `workloads/generated/specint-502-mmap-guard-kernel-20260628-r2/initramfs/502_gcc_r/run_001/qemu.log` moved that mmap to `0x1556274000`, but the failure moved to `memset` at static `0x40b29e4c` with `a0=0x1556273000` and `a2=0x1800`. The list-dump trace `workloads/generated/specint-502-alloc-pcwatch-dump-regs-20260628-r1/initramfs/502_gcc_r/run_001/qemu.log` proves the source of the overlap: oldmalloc trims/bin-chunks both `a0=0x1556272ff0` and `a0=0x1556273010`, producing overlapping free chunks at `0x1556276010` and `0x1556276030`. After allocation from `0x1556276010` returns payload `0x1556276020`, GCC's normal GGC writes at `0x15559361c8..0x15559361e6` overwrite the stale `0x1556276030` free-list node; the final `unbin` then sees chunk words `[0]=0x1000 [1]=0x1556277000 [2]=0x1556273fe0 [3]=0`. The 2026-06-29 exact-PC ring probe `workloads/generated/specint-502-pcwatch-ring-final-20260629-r1/initramfs/502_gcc_r/run_001/qemu.log` captured the now-closed bad RTL/function-pointer block: `[sp+0x118]` and `a0` held `0x1555b44b14`, the `c.ldi [a0+16]` result became `tq0=0x305910060a11b059`, and static slide `0x1515555000` mapped `a0` to `gen_movsi` code (`0x405efb14`), not to an RTL object. `workloads/generated/specint-502-icall-target-fix-20260629-r1` proves the new Linx LLVM ICALL pseudo keeps the indirect target out of ABI argument registers; `workloads/generated/specint-502-icall-target-varfn-fix-20260629-r1` proves the 502-only SPEC variadic-function-table workaround advances past that block to the current `realloc`/`mremap` trap. A 2026-06-29 Linx oldmalloc direct-large-mmap hint experiment was rebuilt and tested under `workloads/generated/specint-502-large-mmap-hint-20260629/focused/stage_b_summary.json`; it reproduced the same earlier stack-2G trap at `addr=0x305910060a11b059` with `heartbeat_running=true`, so that libc-only hint was backed out. The restored relink evidence is `workloads/generated/specint-502-restored-20260629/build_manifest.json`.
  Follow-up: keep the older brk/mmap overlap as historical evidence only unless it reproduces under the current mallocng/default run shape. The later `addr=0x8` trap is closed by the Linx LLVM by-value aggregate lowering fix; the latest all-static train run classifies `502.gcc_r` as heartbeat-backed `live-timeout` with no `LINX_USER_TRAP`.

- [ ] ID: SPEC-M05-LIVE-SLOW The live slow train workloads need QEMU speedups or longer diagnostic budgets.
  Current blockers: the latest initramfs all-static train ledger still splits rows into throughput rows, C++ resource/child-exit rows, and the old 525 rootfs-panic row. The newer 9p fast gate under `workloads/generated/specint-train-all-9p-failtimeout-20260630-r1/` moves all ten supported train rows into `live-timeout` with `heartbeat_running=true`, `heartbeat_site_progress=true`, and `stalled=false`; even `999.specrand_ir` times out under the 180s 9p fast budget. Treat current 9p failures as transport/throughput budget failures, not correctness stops.
  Evidence:
  - `500.perlbench_r`: run_001 hash `0xc69c7085` passes; run_002 count `70000000011`, BPC `0x1555670bca`, `progress=site-change`
  - `502.gcc_r`: count `40000000006`, BPC `0x15556bdf40`, `progress=site-change`, no `LINX_USER_TRAP`
  - `505.mcf_r`: count `77000000005`, BPC `0x155555ccf8`, `progress=site-change`
  - `531.deepsjeng_r`: count `72000000030`, BPC `0x155555ba20`, `progress=site-change`
  - `557.xz_r`: count `65000000007`, BPC `0x1555570608`, `progress=site-change`
  - 9p fast gate: `500`, `502`, `505`, `520`, `523`, `525`, `531`, `541`, `557`, and `999` all classify as `live-timeout`; representative BPCs are recorded in `workloads/generated/specint-train-all-9p-failtimeout-20260630-r1/qemu_matrix_summary.md`.
  - 9p profile/trace: `workloads/generated/specint-profile-999-9p-current-20260630-r1/profile/qemu-999-9p-current.sample.txt` samples `helper_linx_tlb_iall`/`tlb_flush_by_mmuidx_async_work`, but `workloads/generated/specint-tlbtrace-999-9p-20260630-r1/999_specrand_ir/run_001/qemu.log` maps the first invalidations to boot/fixmap helpers (`get_pmd_virt_fixmap`, `get_pud_virt_fixmap`, `get_p4d_virt_fixmap`) before `LINX_SPEC_START`. The corrected post-boot filter run `workloads/generated/specint-tlbtrace-999-9p-postboot-20260630-r2/` emits zero `LINX_TLB_TRACE` lines after 1B instructions while heartbeat reaches 5B instructions with changing BPCs, so that TLBI sample is boot-biased rather than the current SPEC steady-state blocker.
  Note: keep `--stack-limit 2G` in train loops; it still separates historical stack traps from current live-slow and wrapper-exit rows.
  Proposed solution: profile live-slow rows with heartbeat off or very coarse, and either start host sampling after `LINX_SPEC_START` or use QEMU trace count filters so boot setup does not dominate the sample. Target tile set/reset, template-step, page-local BSTART decode caching, TB chaining, wrapper polling, allocator/free-list hot BPCs, and 9p path overhead. Keep a separate boot-performance lane for TLB invalidation: QEMU now page-scopes `tlb.iv` and Linux's packed `tlb.iav`, while `tlb.ia`/`tlb.iall` remain conservative full flushes. A kernel-side fixmap lane can still reduce redundant clear/set fixmap invalidations. The queue-inline sample `workloads/generated/specint-profile-500-queue-inline-20260629-r2/profile/qemu-500-queue-inline.sample.txt` proves scalar queue read and TQ/UQ push helpers are removed; the heartbeat-guard sample `workloads/generated/specint-profile-500-queue-inline-hbguard-20260629-r1/profile/qemu-500-queue-inline-hbguard.sample.txt` proves heartbeat helper overhead is removed; the tile-commit guard sample `workloads/generated/specint-profile-531-tilecommit-guard-20260630-r1/profile/qemu-531-tilecommit-guard.sample.txt` proves scalar no-op tile commit overhead is removed. Historical samples are under `workloads/generated/specint-train-all-20260628-heartbeat-stacklimit/profile/`.

- [x] ID: SPEC-M05-USERTRAP-502-541 Deterministic userspace traps must be split from live-slow work.
  Resolution: the historical deterministic trap lanes have been split from the live-slow profiling lane. The mremap-era `502.gcc_r` trap is closed, the later by-value aggregate `addr=0x8` trap is also closed, `541.leela_r` no longer hits the finite-stack trap under `--stack-limit 2G`, and the old `520.omnetpp_r`/`523.xalancbmk_r` startup/stack traps are not reproduced in the current ledger.
  Evidence: `workloads/generated/specint-train-all-icall-varfn-fix-20260629-r1/train-all/initramfs/stage_b_summary.json` and `workloads/generated/specint-502-icall-target-varfn-fix-20260629-r1/qemu/qemu_matrix_summary.json` capture the now-closed 502 allocator/VM class after the bad RTL/function-pointer path was closed. `avs/qemu/out/mremap-end-smoke-r3/summary.json`, `workloads/generated/specint-502-mremap-fix-20260629-r1/qemu_matrix_summary.json`, and `workloads/generated/specint-train-all-mremap-fix-20260629-r1/qemu_matrix_summary.json` prove 502 now reaches heartbeat-backed live timeout with no `LINX_USER_TRAP`. `workloads/generated/specint-541-stack-unlimited-20260629-r1/541_leela_r/run_001/qemu.log` proves the 541 finite-stack trap disappears under unlimited stack for at least 40.05B guest instructions; the bounded sweep shows `512M` traps at `addr=0x3fdffffff8`, `1G` traps at `addr=0x3fbffffff8`, and `2G` reaches a 240s live timeout. Older focused 505 evidence in `workloads/generated/specint-505-final-faultregs-20260628-r1/initramfs/505_mcf_r/run_001/qemu.log` remains historical unless the latest live-timeout run reproduces that trap.
  Follow-up: keep `520`, `523`, and `541` in the wrapper-output lane until child status is known. Keep `502.gcc_r` in live-slow profiling unless a fresh current run reproduces a user trap.

- [x] ID: SPEC-M05-OLDMALLOC-CPP Early Linx oldmalloc no longer rounds heap growth to zero before libc init.
  Resolution: Linx oldmalloc now falls back to a compile-time page size when `libc.page_size` has not been initialized yet.
  Evidence: `avs/qemu/out/musl-static-oldmalloc-page-20260628/summary.json` passes focused static musl smoke, and `workloads/generated/specint-cxx-after-oldmalloc-20260628/run/initramfs/520_omnetpp_r/run_001/qemu.log` plus `workloads/generated/specint-cxx-after-oldmalloc-20260628/run/initramfs/523_xalancbmk_r/run_001/qemu.log` show first `brk` growth to the next page (`0x27c000` and `0x4f7000`) instead of the earlier no-op `brk(current)`.
  Follow-up: this fixed the allocator symptom only. The later 520 null-constructor trap is now closed by the forced-static startup fix, and current 520/523 ownership is live-slow profiling.

- [x] ID: SPEC-M05-CPP-STARTUP C++ static SPEC executables enter through musl `_start`, not `main`.
  Resolution: the forced-static C++ wrapper links musl startup objects and C++ runtime archives without `-e main`; the build manifest rejects forced-static images whose ELF entry is not `_start`. The build driver now defaults `--force-static` to `LINX_SPEC_LINK_MODE=default` and rejects an explicit legacy forced-static request.
  Evidence: `workloads/generated/specint-cxx-startup-fix-20260628/build_manifest.json` records `static_entry_ok=true` for focused `523.xalancbmk_r` and `541.leela_r`. `workloads/generated/specint-520-cxx-startup-fix-20260629-r1/build_manifest.json` records the focused 520 rebuild with `_start`, `__libc_start_main`, and `__init_array_start/end`; `workloads/generated/specint-520-cxx-startup-fix-20260629-r1/ctor-watch/520_omnetpp_r/run_001/qemu.log` records repeated `ExecuteOnStartup::ExecuteOnStartup` hits and stores to `ExecuteOnStartup::head`, proving static constructors run under QEMU.
  Follow-up: keep `tools/build_linx_llvm_cpp_runtimes.sh --profile spec --mode phase-b` as a prerequisite after any phase-b musl rebuild.

- [ ] ID: SPEC-M05-CPP-RUNTIME C++ train workloads must finish after startup.
  Current blockers:
  - `520.omnetpp_r`: no longer traps at `addr=0` after the forced-static startup fix. The latest 2 GiB all-static train run exits the child with `sig=9`; focused 4 GiB reruns show this is memory/stack-layout sensitive, not C++ startup. With `--memory-mb 4096 --stack-limit 2G`, 520 reaches a user trap at `addr=0x3f7ffffff8` with `sp=0x3f80000000`, exactly one word below the 2 GiB stack floor. With `--memory-mb 4096 --stack-limit unlimited`, it passes that stack floor and later exits with `sig=9` after the child grows to roughly 4.1 GiB virtual/RSS. Focused `999.specrand_ir` probes now show the cutoff is above 4096 MiB: 4 GiB passes, while 4352 MiB, 5 GiB, and 8 GiB panic before userspace with `corrupted stack end detected inside scheduler`.
  - `523.xalancbmk_r`: no longer a C++ stack/startup trap under `--stack-limit 2G`; latest all-static train run exits the child and the wrapper emits `LINX_SPEC_FAIL child-exit`.
  - `541.leela_r`: no longer a finite-stack trap under `--stack-limit 2G`; latest all-static train run exits the child and the wrapper emits `LINX_SPEC_FAIL child-exit`.
  Evidence: `workloads/generated/specint-train-all-latest-qemu-20260630-r1/initramfs/stage_b_summary.json` records child-exit wrapper failures for `520`, `523`, and `541` with `sig=9`, heartbeat progress before exit, empty verified output files, and no `LINX_USER_TRAP`. Focused 520 evidence is under `workloads/generated/specint-520-mem4g-20260630-r1/stage_b_summary.json`, `workloads/generated/specint-520-mem4g-unlimited-stack-20260630-r1/stage_b_summary.json`, and `workloads/generated/specint-520-mem8g-unlimited-stack-20260630-r1/stage_b_summary.json`. Historical startup/stack evidence remains in `workloads/generated/specint-520-cxx-startup-fix-20260629-r1/ctor-watch/520_omnetpp_r/run_001/qemu.log`, `workloads/generated/specint-cxx-stacklimit-20260628/qemu-focused/qemu_matrix_summary.json`, the focused register-trace run under `workloads/generated/specint-cxx-after-oldmalloc-20260628/run/initramfs/`, and the focused `541` sweep under `workloads/generated/specint-541-stack-{512m,1g,2g}-20260629-r1/`.
  Proposed solution: keep C++ rows in a resource/debug lane: first fix or bound the 520 stack/RSS growth under <=4 GiB guest memory, then separately diagnose why guest memory above 4 GiB corrupts the scheduler stack-end sentinel before SPEC userspace. Continue to run 523/541 with guest child heartbeat/proc status plus kernel kill/OOM tracing before reopening compiler prologues, C++ atomics, or QEMU memory state.

- [x] ID: SPEC-M05-PANIC-525 `525.x264_r` first panic cause is captured before the delay loop.
  Current blocker: the initramfs lane still packages a 1.6 GiB CPIO and falls through to `prepare_namespace()` before `/init` is accessible, but the SPEC 9p lane now mounts and reaches benchmark execution. The current 525 9p blocker is `live-timeout`, not rootfs panic or 9p mount failure.
  Evidence: `workloads/generated/specint-train-all-latest-qemu-20260630-r1/initramfs/525_x264_r/run_001/qemu.log` reaches count `43000000002`, BPC `0xffffffff803e88aa`, and recent heartbeat sites `0xffffffff800019bc`/`0xffffffff800019b2` symbolize to `panic.c:0`; no `LINX_SPEC_START` marker appears in the log. `workloads/generated/specint-525-panic-pcwatch-20260630-r1/525_x264_r/run_001/qemu.log` stops at `panic()` with `a0=0xffffffff80886822`, the kernel string `VFS: Unable to mount root fs on "%s" or %s`, and a call ring whose final caller `0xffffffff80402154` is in `init/do_mounts.c`. The historical 9p `-EFAULT` evidence is `workloads/generated/specint-525-9p-rawmount-argdump-20260630-r1/525_x264_r/run_001/qemu.log`. The fixed 9p evidence is `workloads/generated/specint-525-9p-vmgetfeatures-wordfix-20260630-r1/525_x264_r/run_001/qemu.log`, where the 9p feature reaches the guest and the SPEC 9p mounts return zero, plus `workloads/generated/specint-train-all-9p-failtimeout-20260630-r1/9p/525_x264_r/run_001/qemu.log`, which reaches `LINX_SPEC_START` and classifies as `live-timeout`.
  Proposed solution: keep initramfs 525 as a rootfs-size/panic regression sentinel, but use 9p or a future disk-image transport for 525 train execution. The next 525 owner is throughput/output validation on the 9p path; if full correctness still needs too much wall time, add an ext2/virtio-blk SPEC transport so large train inputs avoid both giant initramfs packaging and 9p runtime overhead.

- [ ] ID: SPEC-M05-SHARED-RUNTIME Shared SPEC executables must match the static gate behavior.
  Current blocker: shared phase-b executables all fail quickly in the all-train diagnostic run; even `999.specrand_ir` traps after the rebuild.
  Evidence: `workloads/generated/specint-train-all-20260628-after-kstat/specint_fast_gate_summary.json` and `workloads/generated/specint-train-all-20260628-after-kstat/train-all/qemu_matrix_summary.json`.
  Proposed solution: keep static phase-b as the current correctness gate, and use the shared run only as a libc/loader diagnostic until musl shared startup, `kstat`, and C++ runtime packaging are validated by dedicated static/shared smoke gates.

## Live Blockers (2026-05-21)

- [ ] BLOCK-SPEC-FG-001 Fast SPECint gate must run `test`/`train` before promotion.
  Command: `python3 tools/bringup/run_specint_fast_gate.py --profile pr --spec-dir workloads/spec2017/cpu2017v118_x64_gcc12_avx2 --qemu "${QEMU:-$PWD/emulator/qemu/build-linx/qemu-system-linx64}" --sysroot "${WORKLOAD_SYSROOT:-$PWD/out/libc/musl/install/phase-b}" --out-dir workloads/generated/specint-fast-gate --append-extra "${SPEC_APPEND_EXTRA:-norandmaps}" --guest-heartbeat-sec "${SPEC_GUEST_HEARTBEAT_SEC:-60}"`
  Done means: `test-smoke` and `train-smoke` emit `qemu_matrix_summary.json` artifacts and the aggregate `specint_fast_gate_summary.json`.
  Rationale: this keeps cheap SPECint regressions visible and prevents `505.mcf_r` or `531.deepsjeng_r` stress workloads from hiding faster `test`/`train` signal.
  Policy: smoke suites are the cheap `999.specrand_ir` sentinels; `531.deepsjeng_r` belongs to nightly `test-cpu-stress`/`train-cpu-stress`, and `505.mcf_r` belongs to nightly VM stress.

- [x] BLOCK-SPEC-A-001 Keep the canonical Stage-A matrix command runnable from the superproject.
  Command: `QEMU=/tmp/linx-qemu-clean-build/qemu-system-linx64 python3 tools/spec2017/run_stage_qemu_matrix.py --spec-dir workloads/spec2017/cpu2017v118_x64_gcc12_avx2 --stage a --input-set test --strict --out-dir workloads/generated/spec_stage_a_wrapper_verify_env`
  Resolution: `tools/spec2017/run_stage_qemu_matrix.py` now accepts `--qemu` and defaults from `QEMU` in the environment, then forwards that path to `run_int_rate_qemu.py`.
  Evidence: the earlier failure is still captured in `workloads/generated/spec_stage_a_livecheck/qemu_matrix_summary.json`, while the fixed wrapper now successfully stages a transport run under `workloads/generated/spec_stage_a_wrapper_verify_env/9p/999_specrand_ir/run_001/`.

- [ ] BLOCK-SPEC-A-002 `999.specrand_ir` must complete under both Stage-A transports.
  Command: `python3 tools/spec2017/run_int_rate_qemu.py --spec-dir workloads/spec2017/cpu2017v118_x64_gcc12_avx2 --stage a --transport <9p|initramfs> --input-set test --sysroot out/libc/musl/install/phase-c --qemu /tmp/linx-qemu-clean-build/qemu-system-linx64 --out-dir workloads/generated/spec_stage_a_livecheck_direct/<transport>`
  Blocker: the original shared-runtime route times out on both transports, and the narrowed static fallback still stalls before any guest-visible output. This is now evidence of a broader firmwareless Linux initramfs/userspace-entry blocker, not just a benchmark-local runtime issue.
  Evidence:
  - shared route: `workloads/generated/spec_stage_a_livecheck_direct/9p/stage_a_summary.json`, `.../initramfs/stage_a_summary.json`
  - static fallback: `workloads/generated/spec_stage_a_phaseb_static_999/stage_a_summary.json` (`guest_shared_runtime=false`, `stalled=true`, zero-byte `qemu.log`)
  - libc control lane: `avs/qemu/out/musl-smoke-spec-debug3/summary.json` (`hello_static_runtime_timeout`)
  - no-libc control lane: `kernel/linux/tools/linxisa/initramfs/smoke.py` now also fails against both dirty and sanitized local QEMU builds, so the blocker is below SPEC userspace logic.

- [ ] BLOCK-SPEC-A-003 `505.mcf_r` must complete under both Stage-A transports.
  Command: same as `BLOCK-SPEC-A-002`.
  Blocker: both transports hit the full 240-second guest timeout with no benchmark output files (`inp.out`, `mcf.out`) produced.
  Evidence: `workloads/generated/spec_stage_a_livecheck_direct/9p/stage_a_summary.json`, `.../initramfs/stage_a_summary.json`, and missing-output specdiff logs `.../505_mcf_r_specdiff_1.log` / `..._2.log`.

- [ ] BLOCK-SPEC-A-004 `531.deepsjeng_r` must complete under both Stage-A transports.
  Command: same as `BLOCK-SPEC-A-002`.
  Blocker: both transports hit the full 240-second guest timeout with no benchmark output file (`test.out`) produced.
  Evidence: `workloads/generated/spec_stage_a_livecheck_direct/9p/stage_a_summary.json`, `.../initramfs/stage_a_summary.json`, and missing-output specdiff log `.../531_deepsjeng_r_specdiff_1.log`.

- [ ] BLOCK-SPEC-A-005 The Stage-A subset must reach output generation before specdiff.
  Done means: `rand.24239.out`, `inp.out`, `mcf.out`, and `test.out` exist in the Linx run directories before specdiff executes.
  Blocker: all current Stage-A failures are pre-output guest runtime stalls, not specdiff mismatches after partial progress, and the same silent behavior now reproduces with the static hello control lane.
  Evidence: every current specdiff log reports `Can't open input file ... No such file or directory`.

- [ ] BLOCK-SPEC-A-006 Reconcile stale checklist/docs that still describe `531.deepsjeng_r` as first blocked by missing shared musl.
  Done means: SPEC bring-up docs reflect the current May 21, 2026 stop path.
  Blocker: the stop path has changed twice in one day: earlier shared `phase-c` artifacts were present, then the install tree disappeared, `phase-b` had to be repaired, and the static fallback still hit the same silent runtime stall. The docs need to stop treating the problem as only "`531.deepsjeng_r` needs `libc.so`".
  Evidence: `out/libc/musl/logs/phase-b-summary.txt`, `workloads/generated/spec_stage_a_phaseb_static_999/stage_a_summary.json`, and `avs/qemu/out/musl-smoke-spec-debug3/summary.json`.

- [x] ID: SPEC-M01 Control path ready.
  Canonical plan: `docs/bringup/SPEC_WORKLOAD_PLAN.md`
  Done means: the canonical matrix command is runnable from the superproject, forwards the active QEMU path, and creates per-transport run staging.
  Status: ✅ RESOLVED (2026-05-21) - `run_stage_qemu_matrix.py` now forwards `QEMU=...` / `--qemu ...` to the transport runner.

- [ ] ID: SPEC-M02 Firmwareless Linux userspace entry.
  Canonical plan: `docs/bringup/SPEC_WORKLOAD_PLAN.md`
  Done means: a trivial static initramfs userspace payload emits guest-visible start/pass markers under the same firmwareless Linux/QEMU boot path that SPEC uses.
  Status: ❌ CURRENT FIRST RUNTIME BLOCKER (2026-05-22) - both the corrected static hello control lane and the repo's own no-libc initramfs smoke fail under firmwareless Linux+initramfs boot, so the next runtime blocker is the broader Linux userspace-entry path rather than SPEC harness logic.

- [ ] ID: SPEC-M03 Bringup subset output generation.
  Bringup subset: `spec_policy.bringup_subset`
  Done means: the bringup subset produces its expected output files before specdiff runs.
  Status: ❌ BLOCKED BY SPEC-M02 (2026-05-21) - shared and static narrowed runs still fail before output generation.

- [ ] ID: SPEC-M04 Hosted shared-runtime restoration.
  Command: `MODE=phase-c bash tools/spec2017/build_int_rate_linx.sh --build-runtimes`
  Done means: the shared musl route is restored and dynamic SPEC benches can run against a valid `phase-c` sysroot.
  Status: ❌ OPEN (2026-05-21) - `phase-c` is not the current closure baseline; `phase-b` was repaired first and the shared-runtime path still needs canonical rebuild/revalidation.

- [ ] ID: SPEC-M05 Bringup subset closure.
  Canonical command: `python3 tools/bringup/run_specint_fast_gate.py --profile pr --spec-dir workloads/spec2017/cpu2017v118_x64_gcc12_avx2 --qemu "${QEMU:-$PWD/emulator/qemu/build-linx/qemu-system-linx64}" --sysroot "${WORKLOAD_SYSROOT:-$PWD/out/libc/musl/install/phase-b}" --out-dir workloads/generated/specint-fast-gate --append-extra "${SPEC_APPEND_EXTRA:-norandmaps}" --guest-heartbeat-sec "${SPEC_GUEST_HEARTBEAT_SEC:-60}"`
  Done means: the fast `test`/`train` suites pass qemu + specdiff/hash checks and aggregate summary reports `ok=true`.
  Status: ❌ BLOCKED BY SPEC-M02 / SPEC-M03 (2026-05-21) - transport summaries still show `ok=false` for every bringup-subset bench.

- [ ] ID: SPEC-M06 Promotion-set closure and xcheck readiness.
  Promotion set: `spec_policy.promotion_required`
  Done means: the promotion set passes the promoted transport policy, phase-b static image prep is reproducible, and LinxCore xcheck promotion is actionable.
  Status: ⚠️ NOT STARTED (2026-05-21) - blocked behind SPEC-M02 through SPEC-M05.

- [ ] ID: SPEC-P01 Fortran exclusion policy.
  Done means: `548.exchange2_r` stays explicitly excluded from Linx intrate scope until the project intentionally expands to Fortran/runtime support.
  Status: ✅ POLICY ACTIVE (2026-05-21)
